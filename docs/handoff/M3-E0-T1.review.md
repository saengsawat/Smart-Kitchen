# M3-E0-T1 — Prototype completion: missing MVP screens and non-happy states — review report

**Reviewer:** independent Opus session (read-only, isolated worktree), dispatched by the architect (CLAUDE.md rules 22, 28–29).
**Reviewed:** `m3-e0-t1-prototype` @ `585890c` (first pass) → `6d9c6c4` (re-review). CI runs 35057763214 and 35059752685: **success**.
**Outcome:** **PASS WITH FIXES** (F1–F9 required) → **re-review PASS** with one one-line residual applied by the architect at acceptance. Squash-merged to `main` as `05181fb` (architect, 2026-09-16).

## Verdict summary
Substantial, careful work: all 14 screens exist and are reachable, the six missing S-screens and four non-happy states are real, the copy greps are clean (0 em dashes; no affirmative safety claim; "passed" absent), scope is exactly the three permitted files, CI green, and the worker report disclosed its interpretive calls honestly. It was not accept-as-is: the allergy gate was bypassable by one tap; the S5 ledger screen — whose whole job is teaching ADR-008 — showed history that did not sum to its balance, a clamp with the wrong sign, and a "correct" gesture that overwrote instead of appending; the new blocked states dropped the caveat and evidence, and one blocked row opened the wrong recipe. All nine were strings, numbers, one `onclick` and appended DOM rows; all fixed and re-verified.

## Findings and resolution
| # | Sev | Finding | Resolution (verified at 6d9c6c4) |
|---|---|---|---|
| F1 | High | S2 "Skip" exited the screen without the allergy gate (ticket invariant: not skippable, explicit "none" required) | `skipPreferences()` clears preference chips then calls `continueAllergies()` — gate intact; relabelled "Skip preferences"; old escape hatch gone |
| F2 | High | S5 rows +2.0 / −0.75 / −0.75 vs header 1.25 (sum 0.5); clamp shown as −0.75 (a clamp is a **positive** Adjustment landing the lot at zero); scenario could not clamp | Re-staged: +2.0 Wed · −2.25 Thu (recorded at full magnitude) · **Adjustment +0.25 · corrects the 7:20 pm entry · ≈ Est.** · +1.25 Sat = **1.25 lb** — reconciles; matches `ledger.ts` semantics and provenance tier |
| F3 | High | `correctQty()` / `applyReason()` only toasted and edited the header — an overwrite on the screen that teaches append-only (rule 10, P3) | Both now append attributed, tier-chipped ledger rows; incremental baseline; why-line updated honestly |
| F4 | High | BLOCKED detail block and the all-blocked banner/rows lacked the standing caveat; all-blocked rows lacked evidence (nine rules 2 + 7) | Caveat added to `.verdictblock.blocked`, the banner, and (architect, at acceptance) the normal-mode Thai row; evidence added to both all-blocked rows; cashew row now names the matched term |
| F5 | Med | Cashew all-blocked row opened the Thai peanut detail | Non-navigating honest toast |
| F6 | Med | Shortfall policy presented as settled (rule 3; OQ-1 open) | "How shortfalls are handled is still being decided (OQ-1). This screen shows the proposed policy." on both cook sheets |
| F7 | Med | `.scanmode` dev control inside the phone frame, in terracotta, in the default state | Removed; devbar switch "Scan: match / no match"; default scan screen equals adopted v3 |
| F8 | Med | Scarcity pass incomplete: 15 live terracotta uses, six unaudited, two new | Nine remain: primary CTA, active nav, hero orb/italic/sparkle, loop text, viewfinder brackets + laser (documented camera-chrome exceptions). `.ask .go`, `.sec a`, `.loc.on`, `.leglink`, `.send`, `.pill .dot` de-branded; `.tag.brand` deleted |
| F9 | Med | New fallible values without tier chips (stir-fry row, clamp, missing ingredients, every `.allocrow`) | All chipped; clamp `≈ Est.` per engine provenance |
| trivial | — | Parfait row had no allergen state (absence reads as clearance); ux-plan §7 claimed S4 offline-stale closed | Parfait carries ALLOWED + caveat; §7 marks S4 stale-offline as an open gap |

## Copy audit (final)
Em dash 0 · exclamation 0 in visible copy · `safe`/`passed`/`allergen-free`/`guarantee` 20 hits, all CSS or negations (6× the standing caveat, 2× legend disclaimers, 1× assistant "can't mark any food as safe") · no green check-as-clearance (`.safe` class does not exist) · BLOCKED never rendered with an action (no Cook button, no disabled-with-tooltip, "There is no cook action for a blocked recipe.") · unknowns amber, naming member + severity + locus · `--danger` only in allergen contexts.

## Flow walkthroughs — complete, zero dead taps on MVP paths
1. Onboard → first item: account → allergies (gate) → empty Home → Add → Scan → confirm → populated Inventory.
2. Scan → confirm: hit path; miss → `.miss-panel` (code retained) → manual completion; permission denied → manual fallback (one honest "opens device Settings" toast).
3. Recommend → shop → close the loop: recipe → detail → Cook → FEFO confirm (with shortfall marker) → Inventory; Shopping check-off → undo → "add to pantry?" loop; offline shows stale banner + queued indicator. Gap: no recipe → shopping-list gesture (F13, backlog).

## Coverage vs ux-plan §7
S1, S2, S3 (+ first-run), S4 (+ sort/filter, empty), S5, S6→S9, S7 (+ permission denied), S9, S10 (+ zero-candidates, all-blocked), recipe detail (three verdicts), S11 (+ offline, initials), S12, legend/undo/initials — **present**. Open: S4 stale-offline (recorded), F10–F17 below.

## Scope, format & CI
Against merge-base `92aa59e`: prototype (+785), `ux-plan.md` §7 rows (+ one honest sentence), worker report. Prettier clean on all three files. Both CI runs green.

## Reviewer opinions (labeled; architect disposition in brackets)
- Endorse the S1/S12 split (avatar → S12 steady state; S1 only for new users). **[Kept; ux-plan §1 to say so at T3.]**
- **Backlog, worth a ticket:** recipe detail counts an unconfirmed `AI · confirm` row (mushrooms) toward "have" and the cook sheet deducts it — decide whether unconfirmed AI-tier rows count toward coverage scores and FEFO plans (relates to the M1-T1 observation-boundary follow-up). **[Backlogged as a design/PO decision for M6.]**
- ALLOWED says "4 members" without naming them — an expandable member list would make the count verifiable. **[Backlogged.]**
- Worker §10 items 1–4 accepted; 44px audit re-scoped with measured values (`.leglink` ~15px, `.toastUndo` ~24px, `.chk` 26px, `.okbtn` 32px, `.chip` ~34px, `.sevbtn` 34px, `.loc` ~35px, `.seg` 36px — two of the worst are new in this ticket); `prefers-reduced-motion` absent (laser sweep, screen-in animation); `aria-label` on only two elements. **[Backlogged for M3-E0-T3 / M3 build.]**
- `.allergychip.on`/`.sevbtn.on` spend saturated `--danger` on a selection control (legitimate allergen context); `.noneopt.on` uses a green check beside allergy copy (a declaration, not a verdict, but one glance from the banned pattern). **[Both to M3-E0-T3's scarcity/semantic table.]**
- F12 sort/filter leaves group headers stranded; F13 no recipe→shopping gesture; F14 "No match" reuses the Estimated tier chip for a non-tier status; F16 dead CSS; F17 FEFO never shown as a multi-lot split; `correctQty` prepends while `applyReason` appends. **[Backlogged for the M3 build tickets.]**
- `.pill .dot` → `--ink-3` on the espresso hero: correct (decorative, removes a brand/semantic collision); note the low-contrast pair in T3's table as decoration. **[Recorded for T3.]**

## Confirmation
Read-only worktree; `git status --porcelain` empty at both reviewed commits; nothing modified, committed or pushed by the reviewer.
