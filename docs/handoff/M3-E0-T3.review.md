# M3-E0-T3 — Token sheet finalisation: contrast measurements and scarcity audit — review report

**Reviewer:** independent Sonnet session (read-only, isolated worktree), dispatched by the architect (CLAUDE.md rules 22, 28–29).
**Reviewed:** `m3-e0-t3-tokens` @ `efe1bf9` (first pass) → `d50f9cd` (re-review). CI runs 35112435097 and 35114463618: **success**.
**Outcome:** **PASS WITH FIXES** (F1 required, F2/F3 small) → **re-review PASS**. Squash-merged to `main` as `804881e` (architect, 2026-09-16). *(Note: a first worker attempt stalled before writing anything and was relaunched fresh; nothing from it exists.)*

## Verdict summary
Careful, well-instrumented work. The reviewer re-implemented the WCAG relative-luminance contrast formula from scratch and recomputed all 35 pairs (27 plain + 3 composites + the fix table) — **every ratio matched the worker's to two decimals**, including all 12 proposed fixes recomputed against their targets. Token fidelity is zero-drift (24 `:root` properties hex-for-hex against tokens.md and design-direction §0; design-direction correctly untouched). Tap-target math, terracotta counts and the motion table all check out against the reviewer's own greps. One real coverage gap (F1) and one 1px slip (F2) were fixed and re-verified.

## Findings and resolution
- **F1 — MEDIUM, required (fixed):** two amber/rose text styles render **directly on card backgrounds**, outside tinted chips, and were never measured: `.prov.est` (the Estimated provenance badge, transparent bg, amber text and dashed border) and `.exp.now`/`.exp.soon` (expiry countdown text on `--paper`). Reviewer: amber on paper 3.87 FAIL, amber on sand 3.20 FAIL, rose on paper 3.87 FAIL (body, 4.5 target); `.exp.ok` green on paper 4.88 PASS. **Fix:** five rows added (36–40, incl. green on paper/sand for `.amt.pos`); the proposed `--amber` → `#925b0a` and `--rose` → `#9d4b51` recomputed and confirmed to clear `-bg`, paper AND sand; the `--green` fix revised from `#2d774d` (4.37 on sand — fails) to `#2c744b` (green-bg 4.71, paper 5.49, sand 4.54). Method note added: a systematic pass over every `color: var(--X)` declaration without a co-located background is a follow-up.
- **F2 — LOW (fixed):** `.chip` hit-slop `ceil((44 − 33.6)/2) = 6`, not 5 (computed from the rounded height).
- **F3 — INFO (fixed):** the `--danger` "7 CSS rules" count made derivable: 5 class rules (`.allergen`, `.allergen .sh`, `.allergychip.on`, `.sevbtn.on`, `.verdictblock.blocked`) + 3 inline legend uses = 8. Zero crossover with `--rose` independently confirmed.

## Contrast recomputation (reviewer's own implementation; all match)
ink on sand/paper/sand-2/cream 14.09/17.03/12.38/15.39 PASS · ink-2 6.29/7.60/5.53/6.87 PASS · **ink-3 3.00/3.63/2.64/3.28 FAIL (body)** · `#c9b9a4`, `#d8c9b3`, cream on espresso 8.38/9.88/14.07 PASS · **amber/amber-bg 3.24 FAIL** · **green/green-bg 4.19 FAIL** · **danger/danger-bg 4.93 FAIL (alert 7:1)** · **white/danger 6.49 FAIL (alert)** · ai/ai-bg 4.63 PASS · **rose/rose-bg 3.09 FAIL** · **brand-deep/brand-tint 3.96 FAIL** · **white/brand 3.53 FAIL (15px/700 is below the large-text cutoff)** · **brand-deep/sand 4.07 FAIL** · brand-deep/paper 4.92 PASS · ink-3/espresso 4.29 (decorative, reported only) · white/ai 5.93, white/green 5.03 PASS · composites: cream/toastUndo 8.27, white/glass 9.23, cream/scrim 10.64 PASS (stated mid-tone photo assumption) · rows 36–40 as above. **16 FAILs, every one with a PROPOSED fix that recomputes as passing** (7 distinct token changes: `--ink-3` → `#665c52`, `--amber` → `#925b0a`, `--green` → `#2c744b`, `--danger` → `#882020`, `--rose` → `#9d4b51`, brand-tint pairing → `#a74925`, white on `--brand-deep` for the CTA).

## Token fidelity, terracotta, tap targets, motion (reviewer-verified)
- 24 `:root` properties match tokens.md and design-direction §0 exactly; `design-direction.md` diff empty.
- Terracotta: 10 CSS/inline sites (5 `--brand`, 3 `--brand-deep`, 0 `--brand-tint`, 2 `#f4b48f`) = the table; all KEEP; `.frame i`/`.laser` recorded as camera-chrome exceptions; `.btn.pri` at exactly 16 call sites.
- Tap targets: all 15 heights match (12 FAIL / 3 PASS: `.btn`, `.fab`, `.nav button`); remediation values reach 44px (`.chip` slip fixed).
- Motion: 9 animation/transition declarations = 9 table rows; zero `prefers-reduced-motion` guards today, treatment specified per row.
- Semantic-colour decisions all labelled PROPOSED; dark-mode readiness present.

## Scope, format & CI
Against merge-base `2d2b25f`: `docs/design/tokens.md` (new) and `docs/handoff/M3-E0-T3.worker.md` (new) only. Zero em dashes in both. Prettier clean. Both CI runs green.

## Reviewer opinions (labeled; architect disposition in brackets)
- The arithmetic is unusually strong — 40/40 ratios and all fixes matched on first recomputation; a positive precedent for future audits. **[Noted.]**
- Systematic colour-only pass (every `color:` without co-located background) as a follow-up; `.amt.pos` green on sand 4.04 is a narrow FAIL if that combination co-occurs in shipped markup. **[Backlogged for the M3 token-module ticket, together with adopting or rejecting the 7 PROPOSED darkenings — a PO visual call.]**
- The photo-luminance assumption and the brand-button trade-offs (rows 24/25) are honestly flagged PROPOSED items. **[Recorded.]**

## Confirmation
Read-only worktree; `git status --porcelain` empty at both reviewed commits; nothing modified, committed or pushed by the reviewer.
