# M1-T4 — Allergen rule engine · independent review record (Opus, adversarial)

**Branch reviewed:** `m1-t4-allergens` (round 1 @ `f662cf6`, delta re-check @ `792eff6`) · **Reviewer:** independent Opus agent (did not write the code) · **Committed by the architect at acceptance per CLAUDE.md rule 29.** Safety-critical review: maximally adversarial by design.

## Round 1 verdict (@ f662cf6): PASS WITH FIXES
## Round 2 verdict (delta @ 792eff6): **PASS — accepted**

---

## Round 1 — what held (no block-bypass found)

Every attempt to bypass a *block* failed: smuggled/forged fields at every input level (none change the verdict, none reach the output — explicit field-list construction, the M1-T1 lesson applied up-front); tier spoofing (`"KNOWN_FACT "`, case variants — strict equality fails closed); invented negative assertion kinds (`FREE_FROM`, `DOES_NOT_CONTAIN`… — never clear, always degrade to unknown); exclusion-phrase abuse (crafted strings could not mask a real allergen mention; exclusions never touch the assertion path); false-positive boundaries (`pea`≠`peanut`, `nutmeg`≠tree nut, `buckwheat`≠`wheat`, `eggplant`≠`egg`, `shellfish`≠`fish`); declaration cross-licensing (majors↛user-defined and vice versa); aggregation hiding (one blocked member among 12 → BLOCKED); determinism (25 runs byte-identical); the no-"safe" contract (full result-graph walk; the guard test bites under mutation).

## Round 1 — findings

Three independent **fail-open paths to `ALLOWED`** (the reviewer attacked the licence, not the block):

- **F1 (HIGH):** `ALLERGEN_CODE_ALIASES[folded]` on a plain object resolved prototype keys — `normalizeAllergenCode("constructor")` returned the `Object` function, so a `constructor`-coded assertion looked *recognized*: the `UNRECOGNIZED_ALLERGEN_DATA` and critical `SEVERE_ALLERGY_UNKNOWN_DATA` warnings were cleared and the verdict upgraded to `ALLOWED` under a declaration. Contravened rule 9 / INV-ALRG-2.
- **F2 (HIGH):** the absence licence for user-defined restrictions accepted the product *name* as scannable text (and recipe ingredient refs are mandatory, making it unconditionally satisfied for recipes) — `ALLOWED` reachable with zero ingredient statement scanned, contradicting the module's own stated rationale.
- **F3 (HIGH):** zero-width/format characters (ZWSP/ZWNJ/ZWJ/soft hyphen/LRM/RLM/word joiner/BOM) *split* tokens instead of vanishing — `pea​nut` never matched, and under a KNOWN_FACT declaration reached `ALLOWED`. Reviewer opinion (ratified): this violated INV-ALRG-1 as written and the declaration gate did not mitigate it; invisible-character stripping required now, while homoglyphs (Cyrillic а) and genuine hyphenation are **accepted residual risk** with an M6 suspicious-character detector backlogged (warn, never match).
- **F4 (MEDIUM):** documented-total `screenSubject` threw on malformed subject data (null declaration, numeric texts, non-array allergens); string `allergens` silently iterated characters.
- **F5 (MEDIUM, test gap):** alias folding into a block had zero screening-level coverage — reviewer mutation R8 (fold removed) killed 0 of 128 tests.
- **F6–F9 (LOW/INFO):** one report figure off (mutation M5: 19 vs 20 — later reconciled: rename-variant kills 20, delete-variant 19; both real measurements, both now recorded); a mis-citation of domain-model §4 invariant 7 for the (new) aggregation semantics; a spliced docblock sentence; `bean curd` false-positive on the milk term `curd` (over-blocking direction; exclusion added).
- **P5 design observation:** the `AllergenDeclaration` is the trust root of the permissive verdict yet was unauthenticated caller data — minimum hardening (non-empty `source`) endorsed by the architect and applied; the full who-may-mint policy recorded as an OPEN pre-M4 decision.

Reviewer mutation battery (round 1) confirmed the worker's M1–M4 exactly; independent mutations R1–R8 exposed F5.

## Round 2 — delta re-check (all fixes verified by re-running the original repros)

- **F1:** `constructor`/`toString`/`__proto__`/`prototype`/`valueOf` → `undefined`; a `constructor`-coded assertion now byte-identical in effect to a genuine unknown code, both warnings restored. Real aliases still fold.
- **F2:** both repro shapes → `ALLOWED_WITH_UNKNOWNS` with `NO_INGREDIENT_TEXT`; not over-corrected (real ingredient statement still licenses; MAJOR restrictions unaffected).
- **F3:** all nine invisible-character classes now normalize to the intact token and **BLOCK, bare and declared**; characters deleted before the separator step (the correct order). Documented residuals behave as agreed.
- **F4:** zero throws across 11 malformed shapes, all fail closed. **P5:** empty/whitespace/missing/non-string `source` → `UNSOURCED_DECLARATION`, properly sourced still licenses. **F9:** `bean curd` excluded precisely without over-masking.
- **Mutations independently re-measured:** every reverted fix kills ≥1 test; reviewer's R8 now kills 4 (was 0); worker's counts confirmed (one divergence explained by the reviewer's broader mutation being *more* strongly pinned). Aggregation mutation kills 69.
- **No regression:** test diffs strictly additive (428 added, 0 deleted; one fixture-note line changed in both mirror copies); 216 pre-existing tests untouched; clean declared product still `ALLOWED`, genuine match still `BLOCKED`.
- **Scope/CI:** fix commits touch only `allergens/**`, the `index.ts` docblock, one fixture note, and the handoff report; no deps/configs. CI run `34398257771` green on branch HEAD; full local gate green (379/379).

## Mandatory pre-M4 follow-up (recorded, does not block acceptance)
`parseAssertions`/`asAssertionList` silently discard structurally malformed allergen *containers/elements* (`allergens: "peanut"` / `5` / `["peanut"]`) instead of recording them as uninterpreted — under a valid sourced declaration this reaches `ALLOWED`. Narrower than F1 (needs malformed upstream data AND a valid declaration; cannot un-block), and no adapter mints declarations today. **Must land before any adapter can mint a declaration** (~3 lines + tests). Backlogged as a hard M4 precondition.

## Reviewer opinions (labeled; architect ratified at acceptance — see DECISIONS D-017)
- **P1 cross-contact (`severe: BLOCK`, `standard: WARN_UNKNOWN`, frozen data not caller option): ratify as implemented.**
- **P2 molluscs inside `shellfish`: keep** (over-inclusion blocks; pair with a distinct `mollusc` code at M4).
- **P3 coconut as tree nut: keep for MVP** — but the highest-cost conservative call in the data; pull the separate-code split forward to M4.
- **P4 severe+unknown not escalated at engine level: agree** — INV-ALRG-2 prescribes unknown+warning; the critical warning is M6's filtering hook.
- **P5 `AllergenDeclaration` design: sound** (positive sourced claim as sole licence; two-claim split correct; no cross-licensing) — the fail-open paths were implementation bugs, not design flaws; who-may-mint remains the open trust question.

Tree left byte-identical to origin in both rounds; reviewer committed nothing, merged nothing, touched no status files.
