# M1-T10 — Maintenance bundle (items a–m) — review report

**Reviewer:** independent Sonnet session (read-only), dispatched by the architect (CLAUDE.md rules 22, 28–29).
**Branch/commit reviewed:** `m1-t10-maintenance` @ `26b0d63` (14 commits on `main` @ `66fd4b2`, one per item plus the report). CI run 35051314090: **success** (architect-verified via the public Actions API; the reviewer had no `gh`).
**Outcome:** **PASS** — no fixes required. Squash-merged to `main` as `605aae2` (architect, 2026-09-15). **Closes the D-018 batch (M1-T6 → T10).**

## Verdict summary
All 13 items verify against their acceptance lines, each independently reproduced (digest re-fetched from the registry, GS1 check digit recomputed, Open Food Facts non-collision re-queried, lint failure reproduced, four mutations hand-applied and killed). No product-logic behaviour change anywhere in the diff. Rule 11 holds to exactly the two permitted dependency changes. Gate counts reproduce the worker's exactly, without and with a real Postgres 17 cluster. One LOW finding (F1) and one judgment call, both non-blocking.

## Findings
- **F1 — LOW, item (e), non-blocking:** the new lot-instant canonicalisation is behaviourally a no-op today and its test cannot discriminate the fix — Postgres `timestamptz` + node-postgres already normalise on read, and lots have no in-memory replay comparison (unlike `canonicalizeTransactionInput`, the real M1-T2 F9 case). Harmless, consistent with the transaction-side pattern, but reverting it by accident would not fail a test. Recommend documenting it as defence-in-depth or adding a test on a path where the string form matters. **[Architect: recorded as a note; no ticket.]**

## Judgment call (i) — acceptable, follow-up recommended
The worker re-exported three corpus-data names (`RECOMMENDATIONS_CORPUS_CASE_FILES`, `RECOMMENDATIONS_CORPUS_CASES`, `RecommendationsCorpusCase`) from `packages/domain/src/index.ts` and added `@smart-kitchen/domain` as an adapters devDependency plus a tsconfig reference, because domain's `exports` map exposes only `"."` and a relative import fails `tsc -b` composite rootDir rules. Boundary rules unaffected (adapters → domain is the allowed direction; `test-support.ts` imports nothing; domain stays zero-dependency, zero-I/O). The widening is narrow, documented, data-only — but it is the production barrel. A subpath export (`"./allergens/corpus"`) would keep `"."` exact and make the test-only nature explicit at the import site. **[Architect: accepted as merged; subpath-export migration recorded as a follow-up before domain gains more consumers.]**

## Per-item verification (reviewer's own checks)
| Item | How verified | Result |
|---|---|---|
| (a) | `corepack prepare pnpm@12.3.4 --activate` + `pnpm install --frozen-lockfile` locally; `workflow_dispatch:` present | PASS |
| (b) | digest re-fetched from `registry-1.docker.io` (token + manifest HEAD): `sha256:18cfe3ef…5d73` = ci.yml; bump comment present | PASS |
| (c) | injected `import fc from "fast-check"` into `taxonomy.ts` → exact `import-x/no-extraneous-dependencies` failure; reverted; comment no longer overclaims | PASS |
| (d) | `npx eslint scripts/` clean, no parsing errors | PASS |
| (e) | diff scoped to the two `canonicalizeInstant` calls; mutation-reverted → tests still pass (F1) | PASS (F1) |
| (f) | `pure.test.ts` has no DB gate; 31 tests execute without `DATABASE_URL` | PASS |
| (g) | mutated `driftCheck`'s `next_sequence` comparison → new test fails; reverted | PASS |
| (h) | grep: helper imported only by its test; RFC 9562 bits hand-checked; 10 000 ids unique, strictly increasing, version 7 / variant 10 | PASS |
| (i) | 19-test consistency suite passes; fixture field mutated in repo → drift caught; reverted; README points at real files | PASS |
| (j) | GS1 check digit recomputed (`060000100810` valid); OFF re-queried → `status: 0`; corpus assertion is full sorted-set equality incl. `sesame` | PASS |
| (k) | CONTRIBUTING section matches the `initdb`/`pg_ctl` pattern the reviewer used | PASS |
| (l) | arity-preserving mutation of the item-level `WHERE household_id = $1` → mismatched-id test kills it; Beta identity assertion and view/read parity property present; no SQL touched | PASS |
| (m) | `try/catch` around `cause`; `delay()` validated; "delete `throw sleepError`" mutation kills all 5 new tests | PASS |

## Dependency & scope check
`git diff 66fd4b2..26b0d63 -- package.json pnpm-lock.yaml '**/package.json'`: exactly `packageManager` 12.3.1 → 12.3.4, `packages/adapters` devDependency `@smart-kitchen/domain: workspace:*`, and lockfile consequences. Per-commit file scope matches the ticket bullets. No `.gitignore` change; no stray build artifacts under `packages/domain/src` (the worker's self-reported clean-up confirmed).

## Gate results (reviewer-run)
| | Without `DATABASE_URL` | With `DATABASE_URL` (Postgres 17, port 55441) |
|---|---|---|
| `pnpm test` | **42 files · 665 passed / 114 skipped (779)** | **42 files · 769 passed / 10 skipped (779)** |
| lint / typecheck / format:check | clean | clean |
| `pnpm audit --audit-level=high` | no known vulnerabilities | — |
| `pnpm install --frozen-lockfile` | up to date | — |

Both counts match the worker's exactly (+71 tests over the 594/697 baseline).

## Confirmation
Repo restored to `26b0d63` after each mutation; back on `main`; `git status --short` shows only the Excel-held `BACKLOG_TRACKER.xlsx`; throwaway cluster stopped and deleted; the installed PostgreSQL service never touched. Nothing committed, pushed or merged by the reviewer.
