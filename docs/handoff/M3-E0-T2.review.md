# M3-E0-T2 — Safety and provenance copy deck — review report

**Reviewer:** independent Opus session (read-only, isolated worktree), dispatched by the architect (CLAUDE.md rules 22, 28–29).
**Reviewed:** `m3-e0-t2-copy-deck` @ `da49eda` (first pass) → `a69c349` (re-review). CI runs 35057552458 and 35058682029: **success**.
**Outcome:** **PASS WITH FIXES** (F1–F5 required; F6–F10 applied; F11–F14 optional, applied) → **re-review PASS**. Squash-merged to `main` as `b4e4c4f` (architect, 2026-09-16). Three editorial nits (R1–R3) applied by the architect at acceptance.

## Verdict summary
A strong deck treated like code. Coverage is complete against the real unions (63/63 machine codes, nothing invented, `MALFORMED_ALLERGEN_DATA` and `NOT_FINITE` present); grep gates clean (0 em dashes, 0 exclamation marks, every `safe`/`guarantee` hit is a quoted rule, a "forbidden alternatives" cell, or a declared negation); scope exactly the two ticket files; the worker's judgment calls all checked out and it correctly refused to resolve OQ-1/A9. Five findings had to be fixed before the deck could bind the M3 string table, because each would have pushed a wrong safety behaviour into the client.

## Findings (first pass) and their resolution
- **F1 — MAJOR (fixed):** §3.2 waived the standing `NO_SAFETY_GUARANTEE` caveat on recipe cards, contradicting nine-rule 2, the deck's own §2, and the adopted prototype; its "appears in the legend" cross-reference was false. Now: the caveat renders under the verdict line on every surface, every row, "never deferred to the tap-through", including an already-BLOCKED card.
- **F2 — MAJOR (fixed):** the `UNRECOGNIZED_ALLERGEN_DATA` string asserted "It didn't change this result" while being rendered on `ALLOWED_WITH_UNKNOWNS`/`BLOCKED`, where it is false (the engine's own message: "affected restrictions are reported as unknown"). Now verdict-neutral, with the narrower F7 variant used only when nothing else is unresolved.
- **F3 — MAJOR (fixed):** the `MALFORMED_ALLERGEN_DATA` tone note claimed an unknown line can co-occur with household `ALLOWED`. Wrong: household verdict = worst member verdict, any unknown forces `ALLOWED_WITH_UNKNOWNS`; only the *warning* co-occurs with `ALLOWED` (M1-T6 F7). Corrected verbatim.
- **F4 — MINOR/MAJOR (fixed):** compact surfaces defined no treatment for `critical`/`high` warnings. Now: severe card headline variants and "high and critical warnings are never deferred to the tap-through".
- **F5 — MINOR (fixed):** the caveat was not the adopted prototype form. Canonical everywhere now: **"Known matches only · not a guarantee this food is safe."**; the adopted "· label data" third segment documented as a per-surface evidence-basis segment.
- **F6–F10 (fixed):** headline member-count variants at n = 1/2/3+; clamp string placeholders + P3 framing ("Our record was {qty} {unit} short of what you used. Inventory corrected to match."); `NOT_FINITE` row in §8; §7 scoped honestly to S3/S4/S7/S10/S11; assistant BLOCKED line reports, never advises.
- **F11–F14 (optional, fixed):** unevidenced "most people are stocked in under a minute" claim removed; kitchen vocabulary ("batch", "the label's claim to list every allergen"); §10 guarantee note; **P9 added to the binding list with an accessibility-label rule** (verdict label = verdict line + caveat; chip label = "{tier}. {legend line}."; icons decorative).
- **R1–R3 (re-review nits, architect-applied at acceptance):** §3.3 rationale count ("the two rows" → only `ALLOWED` folds the clause in); §3.1 alignment note said design-direction used a full stop (it used an em dash — fixed there too); §10 duplicated lead sentence removed.

## Coverage diff (reviewer-enumerated from source at the reviewed commit)
`ScreeningVerdict` 3 · `RestrictionOutcome` 4 · `UnknownReason` 8 · `WarningCode` 5 · `WarningSeverity` 3 · `EvidenceKind` 5 · `RestrictionSeverity` 2 · `TransactionType` 8 · `ProvenanceTier` 3 · `Actor` 3 · clamp 1 · shortfall 1 · `SkippedLotReason` 2 · `LedgerErrorCode` 15 = **63**. None missing, none invented; unknown-reason copy matches each member's source docblock. Not required and not covered: `StorageLocation` display strings (see notes).

## Grep results (reviewer-run, both passes)
Em dash 0 · en dash 0 · `!` 0 (whole file) · forbidden words: 21 hits, all in permitted cells (quoted rules, forbidden-alternatives columns, the two negations, assistant exception rows, code identifiers, §10's own table) · no colour-word dependence in any live string · sentence case throughout (tier labels named as an exception).

## Nine-rules checklist (final)
1 never "safe"/clearance ✅ · 2 caveat wherever a verdict is shown ✅ (after F1) · 3 unknowns never collapsed, always say what ✅ · 4 critical prominent ✅ (after F4) · 5 keyed off codes ✅ · 6 BLOCKED never a choice ✅ (after F10 in the assistant voice) · 7 evidence always ✅ · 8 per-member attribution ✅ (after F6 headlines) · 9 unknown ≠ absent ✅ · D-017 P1/P4/P5 wording ✅ · M1-T6 F7 rendering ✅ (after F2/F3) · OQ-1 marked pending, not decided ✅.

## Scope & CI
Against merge-base `92aa59e`: `docs/design/copy-deck.md` (+377) and `docs/handoff/M3-E0-T2.worker.md` (+153) only. Both CI runs green (format check is the markdown gate).

## Reviewer opinions (labeled; architect disposition in brackets)
- Accept after F1–F5; F6–F14 cheap and prevent downstream divergence. **[All applied by the worker; accepted.]**
- design-direction §7 quotes the caveat with an em dash while the prototype uses `·` — the repo stated the app's most-repeated safety string three ways. **[Fixed at acceptance; `·` form canonical.]**
- `StorageLocation` display strings (FRIDGE/FREEZER/PANTRY/OTHER, "Other" especially) are user-visible in M3 and owned by no document. **[Backlogged for the M3 string-table ticket.]**
- Worker's four backlog items sound (allergen pluralisation pass; ratify the reason-chip mapping; resolve OQ-1/A9 wording before M8; undo-window duration as a product parameter) + a fifth: accessibility label strings for verdicts and chips. **[Backlogged; the P9 rule now exists in the deck, strings themselves follow with the M3 string table.]**
- Two forbidden-word exceptions (the caveat and the assistant's "can't mark any food as safe"), not one — the worker's judgment call was right. **[Endorsed.]**
- §5's reason-chip mapping ("Cooked" = `USE_IN_MEAL`, "Used" = `CONSUME`) is the right reading of brief §7, correctly labelled ENGINEERING INFERENCE. **[Ratify when the consumption flow is designed.]**

## Confirmation
Read-only worktree; `git status --porcelain`, `git diff --stat`, `git diff --cached --stat` empty at both reviewed commits. Nothing modified, committed, pushed or stashed by the reviewer.
