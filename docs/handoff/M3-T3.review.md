# M3-T3 review: inventory list (S4), item detail with ledger history (S5), HTTP read client, correction and removal behind the client port

**Reviewer:** independent Opus session, read-only. **Branch:** `m3-t3-inventory` @ `790bf3c`. **Baseline:** `main` @ `cb0dd7f`. Committed by the architect at acceptance (CLAUDE.md rule 29); rulings and the re-review outcome are appended at the end.

## Verdict

**PASS WITH FIXES.** The ledger core is right: clamp semantics match ADR-008 and domain-model §4 invariant 2, undo is a compensating `ADJUSTMENT`, nothing edits or deletes a row, the chicken-breast fixture reproduces +2.0, −2.25, clamp +0.25 (system, `OVER_CONSUMPTION`), +1.25 = 1.25 lb, and every quantity path is `bigint` or exact decimal text. No domain import, no `node:fs` in the bundle, all five commands green. Blocking as-is: the "float never enters" test does not discriminate (F1); one required §7 S4 state is unreachable (F2); a failed correction reports success and offers to undo an unrelated row (F3).

## Findings

**F1 (major, rule 13 + ticket invariant)** `quantity.test.ts:45-54` passes whether or not the formatter uses a double; two float mutations left 70/70 green. Fix: add cases a double cannot represent (`12345678901234567.250000`).
**F2 (major, acceptance criterion)** `inventory.tsx:94`, `list-view.ts:96-106`: location-empty and filtered-empty are the same condition, so §7's "No items here yet." is dead code. Fix: branch on the filter that caused the emptiness.
**F3 (major, P8 + rule 19)** `[itemId].tsx:107-120`: `catch {}` swallows every rejection, then "Corrected. Undo" shows with `history.at(-1)` as the undo target; reachable with a live `HttpApiClient` since writes delegate to a fixture with different ids. Fix: toast only on success, take the row id from the write result, show §8 copy on failure.
**F4 (major, copy)** Removal reason stored in `correlationLabel` renders "Discarded in Spoiled" and "used in Spoiled". Fix: action label plus a `reason: {lowercased}` caption (prototype); a `reason` field is an M2-T2 follow-up.
**F5 (major, P8)** `inventory.tsx:46-58`: first load has no `.catch`; cold-start offline leaves a blank screen forever.
**F6 (medium, rule 13)** No test covers the reason-chip to `TransactionType` table (mutation to DISCARD stayed green).
**F7 (medium)** Removal at zero balance throws `ZeroDeltaError` unhandled.
**F8 (medium, P9)** The row Pressable's label swallows the chip's label; tier never announced on S4 rows.
**F9 (medium, precedence)** Row chips ship the prototype's compact "✓ Fact" / "≈ Est." / "AI · confirm" while §4 lists "Known Fact" / "Estimated" / "AI"; the AI chip on rows is inert text, §4 says it is a Confirm action. Architect ruling requested.
**F10 (low)** Clamp row carries no tier chip (its provenance is ESTIMATED).
**F11 (low, copy)** `fixture-seed` renders as user-visible source text.
**F12 (low)** History renders newest-first while the why line is chronological; the prototype list reads oldest-first with corrections prepended. Architect call.
**F13 (low)** `USE_IN_MEAL` renders "Cooked in {recipe}" (deck §5 vocabulary) where the prototype says "Used in". Ruling to record.
**F14 (low, rule 15)** `tests/fixtures/identity/README.md:47` still points at the deleted adapters fixture module (outside worker scope).
**F15 (low)** HTTP body cast without a shape guard.
**F16 (low)** Post-removal `router.back()` unguarded on a deep-linked S5.
**F17 (low, judgement)** Legend intro reworded to clear the substring gate on "safety"; the prototype sentence is a negation of a safety claim, the class §10 already exempts. Recommend the exemption, not the rewording.

## Ledger attack log

Mutations in an isolated scratch copy (70 baseline tests). No floats: `String(Number(...))` in `trimAmountText` and a float `microsToAmountText` both left 70/70 green (F1). Append-only: `appendUndo` splicing the row fails 1 test. Clamp row: actor changed to a user fails 2 tests incl. the chicken-breast reproduction. Reason mapping: EXPIRE to DISCARD left 70/70 green (F6). Source: `quantity.ts` has no `Number(`, `parseFloat` or `.toFixed` on a quantity; `parseMicros` is the single `BigInt(text)` site; `ledger.ts` has exactly three `push` sites and no splice or index write; `currentMicros` is a `bigint` reduce; the clamp is recorded at full stated magnitude then compensated with the exact residual, `systemFlag` set only there, actor `{kind:"system"}`; clamp rendering is icon plus "auto" text plus the §5 sentence, no initials, not colour alone.

## Copy

Verbatim: S4 first-run empty, §7 filtered-empty and stale banner, tray heading, chips and segment labels, header line, correction card text, "Corrected. Undo", clamp string, why template, reason chips and sub-reasons, legend tier lines and closing line. Unreachable: §7 location-empty (F2). Broken downstream: removal rows (F4). Reworded: legend intro (F17). Precedence conflict: row chips (F9), `USE_IN_MEAL` label (F13). Not in the deck: "Search · not in mockup", "Edit · not in mockup", "This item is no longer available." Copy scan covers the new files (JSX text included; mutation caught).

## Acceptance criteria

PASS: grouping, filters, sort without stranded headers, needs-confirmation filter, S5 quantity and lots, chicken-breast reproduction, HTTP list read with bearer (mocked fetch), warm stale banner, legend lines, empty-state build green, append-only and undo (mutation-verified), clamp row, no `danger` on inventory and `rose` only for use-today, 44pt everywhere, reduced motion, no domain import (source and bundle), no new dependency, single `process` exemption, no secrets, contracts consistency (`toEqual` on the ordered list), placeholders removed, adapters dependency removed, bundle carries the new strings and no `node:fs`. FAIL or PARTIAL: §7 states (F2), float test (F1), correction failure path (F3), removal copy and zero-balance and deep-link back (F4, F7, F16), cold-start failure (F5), tier chips (F9, F10), pre-redirect frame proven by inspection only (worker deviation 10).

## Commands

Worktree, warm build state: `pnpm lint` green; `pnpm typecheck` green; `pnpm test` 71 files, 1183 passed / 166 skipped (matches the worker); `pnpm --filter mobile export` green (iOS 2.5 MB, Android 2.8 MB); `pnpm format:check` green. No test deleted or weakened without a moved replacement.

## Scope and deviations

Outside scope: the deleted adapters fixture module and test plus the index export (declared, accept, ratify), `packages/contracts/src/inventory.test.ts` (undeclared, forced by the type removal, accept), lockfile (accept). The ten declared deviations: 1 accept; 2 matches the ticket, the harm is on the screen's error handling (F3); 3 accept procedurally, recommend the exemption (F17); 4 accept; 5 accept the field reuse, fix the copy (F4); 6 ruling (F13); 7 accept (no removal undo, out of scope); 8 accept (OQ-1); 9 accept; 10 accept with the standing gap: adopt a component test library (rule 11) before M3-T4 or the M3 screen set ships without component tests. Carried forward: the M2-T1 metro.config comment follow-up was not done (outside scope).

## Opinions (non-blocking)

`formatDraftAmount` duplicates `microsToAmountText`; `appendUndo` has no idempotency (M2-T2 will need a key); `amtPositive` green on sand is the 4.04 contrast case now on a real screen; no expired state ("use today" covers negatives); an AI-tier item reached by deep link to S5 has no Confirm; `src/config/env.ts` has no test.

## Re-review (same reviewer, commit `1f3541b`)

**PASS.** F1 float discriminators now fail under both float mutations; F2 both empty states reachable with an `emptyCause` and four tests; F3 toast only on success with the row id from the write result and §8 copy on failure; F4 removals render the action label plus `reason: {lowercased}` in the row and the why line (reason carried in `provenance.source`, `correlationLabel` left for recipes); F5 cold-start failure renders "Couldn't load your inventory." plus the §8 fallback and Try again; F6 chip-table test fails on a mutated pair; F7 zero-balance chips disabled and caught; F8 tier announced on rows; F9 the AI chip on rows is a sibling 44pt Confirm; F10 clamp row has its tier chip; F11 no internal source tokens (test and bundle); F15 body guard with a table test; F16 `canGoBack` guard; F17 legend sentence byte-identical to the prototype, allow-list an array with whitespace normalisation; F18 search icon gone, tray Edit opens S5, zero "not in mockup" in the bundle. Commands green: lint, typecheck, test 1222 passed / 166 skipped (76 files), format:check, mobile export. Diff scope: 24 files, all fix-driven. Cosmetic leftover: unused `iconButton`/`iconGlyph` styles in inventory.tsx.

## Architect acceptance (2026-09-22)

Accepted and squash-merged. Rulings recorded on the ticket and in the deck: compact row chips with the AI chip as Confirm (deck §4); the legend sentence as a third named negation (deck §10); S5 states, removal captions, history order and "Cooked in" vocabulary (deck §7 S5); F14 README path fixed on main. Scope ratified: deleted adapters fixture module and test, `packages/contracts/src/inventory.test.ts`, lockfile. Follow-ups recorded in BACKLOG "Accepted follow-ups" (M2-T2 reason field, idempotency key, item-detail endpoint, ledger-derived lots, pack-size field; M3-T4 component test library approval, global toast host, Confirm from S5, metro comment; S5 expired state; PO token call on green amounts).
