# M3-E0-T1 — Worker report: prototype completion (missing MVP screens + non-happy states)

**Ticket:** BACKLOG.md → M3-E0 tickets → M3-E0-T1. **Branch:** `m3-e0-t1-prototype`. **File scope used:** `docs/design/mockups/smart-kitchen-prototype.html`, `docs/design/ux-plan.md` (§7 status column only), this report. No other file touched.

## 1. Summary

Extended the single-file prototype with the six missing screens (S1, S2, S5, S9, S12, recipe detail), the four non-happy states, and the shared components ux-plan §7 listed as gaps. Everything lives in the same file, reusing the existing tokens, `.card`/`.prov`/`.verdict`/`.allergen`/`.tray`/`.nav`/`.sheet`/`.chips`/`.srow`/`.loop`/`.ringbadge` classes, and the existing `go()`/`goBack()` back-stack. Non-happy states are driven by four dev switches added above the phone frame (outside the phone chrome, explicitly labelled as not part of the product UI, per the ticket's own suggestion). Nothing outside `docs/design/mockups/` or the ux-plan status column was changed; `docs/design/copy-deck.md` (the sibling worker's file) was not touched or read.

## 2. Screen table

| id | Screen | What was added | Entry points |
|---|---|---|---|
| `scr-home` | S3 Home (existing) | first-run empty teaser (`.firstrun-only`), avatar rewired to `avatarTap()` | five-tab nav |
| `scr-inventory` | S4 Inventory (existing) | first-run empty state, sort-by-expiry chip, needs-confirmation filter chip, provenance legend link, every row now taps through to S5 | five-tab nav |
| `scr-add` | S6 Add hub (existing) | "Type it in" now goes to S9 instead of a toast | FAB |
| `scr-scan` | S7/S8 Barcode scan + confirm (existing, combined as before) | camera permission-denied panel, match/no-match dev toggle revealing a "no match" sheet with the scanned code retained | Add hub → Scan barcode |
| `scr-recipes` | S10 Recipes (existing) | zero-candidates state, all-blocked state, card taps wired to recipe detail, match badge de-branded | five-tab nav |
| `scr-shopping` | S11 Shopping (existing) | offline banner + queued check-off indicator, who-checked-off initials, loop banner de-branded | five-tab nav |
| `scr-assistant` | Assistant preview (existing, unchanged) | — | Home header |
| `scr-legend` | Shared provenance legend (new) | one-time explainer for the three tiers + the allergen tag | Inventory header link, S5 "why" line |
| `scr-account` | **S1** account + household (new) | sign-in gesture (stubbed), create-household / join-by-code cards | Home avatar, when the "New user" dev switch is on |
| `scr-allergies` | **S2** allergies & preferences (new) | FDA nine chips + severity picker, custom-allergen input, explicit "none" gate blocking Continue, skippable preferences | onboarding (from S1), and from S12 "edit" rows |
| `scr-profile` | **S12** profile & household (new) | member list with initials and allergy summaries, invite stub, sign out | Home avatar, in the steady (returning-user) state |
| `scr-manual` | **S9** manual add / completion (new) | name field, mass/volume/count unit-kind picker with unit chips, location chips, every field badged Known Fact, barcode-retained note when arriving from a scan miss | Add hub "Type it in", scan miss panel, camera-denied panel |
| `scr-item` | **S5** item detail / history (new) | plain-language "why 1.25 lb" line (the brief's 2.0 − 0.75 = 1.25 lb worked example), one-tap quantity correction with undo, consume/discard/expire/donate reason chips, lot breakdown with freshness ring, ledger history including the clamp row rendered as a system row | any Inventory row |
| `scr-recipe-detail` | Recipe detail (new) | hero, per-ingredient have/short/missing rows with provenance, one verdict block per variant (ALLOWED / ALLOWED_WITH_UNKNOWNS / BLOCKED), "Why this?" factors, Cook this → FEFO deduction confirm sheet with a record-and-flag shortfall case; no Cook action at all in the BLOCKED variant | any recipe card (Home tonight card, Recipes top card, Miso salmon row, Thai noodles row) |

All 12 ux-plan S-screens plus the recipe-detail and shared-legend screens are present as distinct `<section class="screen">` blocks; `grep -c 'id="scr-'` variants above confirms 14 unique ids, all reachable from the five-tab shell (directly or via one intermediate tap).

## 3. The three make-or-break flows (tap sequences, zero dead taps)

**Flow 1 — Onboard → first item.** Dev switch "New user" → S1 (`scr-account`) → tap "Create household" → S2 (`scr-allergies`, Maya's peanut/sesame severe pre-set) → tap "Continue" (allowed immediately, since a non-empty allergy set already satisfies the not-skippable gate; tapping straight to "No known allergies" also passes) → S3 Home, first-run state (`scr-home.firstrun`, empty-inventory teaser) → tap "Scan your first item" → S6 Add hub (`scr-add`) → tap "Scan barcode" → S7/S8 (`scr-scan`, match sheet already showing) → tap "Add 2 to Fridge" → `confirmAdd()` clears the first-run state and lands on S4 Inventory, now showing the seeded, populated list (the closest a static prototype can get to "a new item now exists").

**Flow 2 — Scan → confirm (the trust builder).** S6 → "Scan barcode" → S7/S8 default (matched) state: Known Fact chip on identity, Estimated chip on best-by, per-field allergen note, quantity stepper, location chips → "Add … to Fridge" → S4. Branch A (no match): tap "Simulate: no match" in the scan header → the sheet swaps to the miss panel (scanned code shown, retained-for-enrichment copy) → "Enter it manually" → S9 (`scr-manual`, code-retained note visible) → fill fields (each badged Known Fact) → "Add to inventory" → S4. Branch B (permission denied): dev switch "Camera denied" → S7/S8 shows the permission-denied panel instead of the viewfinder → "Enter manually" → S9 (no code note) → "Add to inventory" → S4.

**Flow 3 — Recommend → shop → close the loop.** Recipes tab → tap the top card → recipe detail (`scr-recipe-detail`, ALLOWED variant) → "Cook this" → cook-confirm sheet lists the FEFO allocation per ingredient (including the shortfall banner for chicken: "This recipe needs 2 lb… only 1.25 lb is on hand… record and flag") → "Record & flag, log meal" → toast + S4. Separately, the shopping-list loop: Shopping tab → check off "Chicken breast" → toast with Undo → the existing "Olive oil checked off · add it to the pantry?" banner → "Add" → toast confirming the pantry add (unchanged from the pre-existing pattern, which also does not navigate away — kept for continuity). Offline variant: dev switch "Offline" → Shopping shows the stale banner and check-offs read "queued · will sync" instead of "checked off by DC".

## 4. Grep / acceptance table

| Check | Command | Result |
|---|---|---|
| Em dashes | `grep -c "—" docs/design/mockups/smart-kitchen-prototype.html` | **0** |
| "safe" / "passed" / "allergen-free" / "guaranteed" | `grep -noi "safe\|passed\|allergen-free\|guaranteed" ...` | 10 matches, all negated/explanatory — 2 in `env(safe-area-inset-*)` CSS (unrelated, not copy), 1 CSS comment, 5 instances of the standing caveat "not a guarantee this food is safe" / "not a safety guarantee", 1 "never a safety check by itself" (legend), 1 "never claims a food is guaranteed" (legend), 1 the assistant's "I can't mark any food as safe". **None are affirmative safety claims.** No new violation introduced. |
| Prettier | `npx prettier --check docs/design/mockups/smart-kitchen-prototype.html docs/design/ux-plan.md` | pass. Repo-wide `pnpm format:check` currently fails only on an unrelated, untracked `.claude/worktrees/agent-.../ci_check.json` (agent-harness artifact, pre-existing, not created by this ticket, outside its file scope — `docs/` is itself excluded from Prettier by `.prettierignore`, so this file was never subject to the check in the first place). |
| HTML tag balance | scripted open/close tag walk (Node, ignoring void elements) | 0 unmatched tags, 0 mismatches |
| JS syntax | `new Function(scriptBody)` | parses without error |
| Screen ids | `grep -o 'id="scr-[a-z-]*"' \| sort \| uniq -c` | 14 unique ids, each exactly once |

## 5. Verdict states and the clamp row

- **ALLOWED** appears on: Home tonight card, Recipes top card, recipe-detail `v-ok` variant (chicken stir-fry) — "No known allergen match · 4 members · label data" + the standing caveat, neutral ink, no green check.
- **ALLOWED_WITH_UNKNOWNS** appears on: Recipes list (miso salmon row) and recipe-detail `v-unk` variant — amber, names the member (Maya), the allergen (sesame, severe) and what is missing (no ingredient statement for miso).
- **BLOCKED** appears on: Recipes list (Thai peanut noodles, greyed) and recipe-detail `v-blocked` variant — red `.allergen` bar with evidence ("matched 'peanuts'…"), and there is no Cook button rendered at all in that variant (not disabled — absent), with an explanatory line stating a blocked recipe cannot be cooked or planned.
- **Clamp row** appears in S5 (`scr-item`) as `.ledgerrow.sys`: dashed border, italic muted text, no actor initials chip, reading "System correction. Recorded 0.75 lb more than we had on hand. Inventory was corrected to match reality. This line was not entered by a person." — visibly distinct from the two ordinary user-attributed rows above it.

## 6. Scarcity pass — every terracotta (`--brand`) use and why it was kept or changed

| Use | Before | After | Rationale |
|---|---|---|---|
| Match badge ("92% match") | `.tag.brand` | `.tag.glass` (neutral) | Ticket: "match badges → neutral glass". Changed on Home and Recipes tonight cards. |
| Scan tile (Add hub) | `.mode.bar` terracotta | espresso (`.mode.rec`'s look) | Ticket: "scan tile → espresso". |
| Loop banner (Shopping) | `.loop` brand-tint background, brand button | sand background, brand-deep **text only**, neutral button | Ticket: "loop banner → sand with brand text only". |
| Checked-off chip | green | unchanged (green) | Ticket: "checked-off state stays green" — not terracotta, not touched. |
| Primary CTA (`.btn.pri`) | terracotta | **kept** | Explicitly allowed by the scarcity rule ("primary CTA"); this is the app's one CTA colour. |
| Active nav indicator (`.nav button.on`) | terracotta | **kept** | Explicitly allowed ("active nav indicator"). |
| Hero italic accent (`h1 em`, "tonight") | tint `#f4b48f` | **kept** | Explicitly allowed ("hero italic accent"). |
| FAB (Add button) | espresso, not brand | unchanged | Already scarce; not a ticket item. |
| Location chip selected state (`.loc.on`), scan-confirm and manual-add | terracotta border/tint | **kept** | Not named in the ticket's fix list; design-direction §2 explicitly allows brand on "focus states" and this is a small form-control selection indicator, not a background or large surface. Flagged here for the reviewer to confirm this reading is correct — could be argued either way. |
| Scanner viewfinder brackets/laser sweep (`.frame i`, `.laser`) | terracotta | **kept** | Decorative chrome tied directly to the live scan action, not a background/large surface; not named in the ticket's fix list. Flagged for reviewer. |
| "Ask your kitchen" pill's circular go-button (`.ask .go`) | terracotta | **kept** | Read as a primary-action button inside the hero, consistent with the CTA allowance; not named in the ticket's fix list. Flagged for reviewer. |

## 7. Copy decisions made without a resolved copy deck (flag for the M3-E0-T2 reviewer)

The sibling ticket (M3-E0-T2, safety-copy deck) was running in parallel and its file was not read. Every new string below is original copy written against P5/P11 and M1-T4 §6, but none of it has been checked against the copy deck yet:

1. Provenance legend wording ("Known Fact… Estimated… AI interpretation… Allergen alert…") — new, needs a copy-deck pass.
2. S2's not-skippable gate message: "Select at least one allergen, or confirm none, to continue." — functional, untested against tone rules beyond em-dash/safe-word greps.
3. S5's clamp-row copy ("Recorded 0.75 lb more than we had on hand. Inventory was corrected to match reality. This line was not entered by a person.") — written specifically to satisfy "visibly not a user statement"; wording choice, not from an existing source.
4. Cook-confirm shortfall copy ("Record what we have and flag the shortfall?") implements OQ-1's proposed policy in UI language; OQ-1 itself is still only PROPOSED (domain-model.md, M1-T8), so this string should be revisited once/if that policy is DECIDED.
5. Camera permission-denied and barcode-miss copy are new and unreviewed.

## 8. Design decisions / simplifications (disclosed honestly)

- **S1 vs S12 split.** The ticket names both "S1 … reached from the Home avatar" and a separately-reached S12. Read literally that is under-specified (both would compete for the same tap target). I resolved it as: the avatar routes to **S12** (profile & household) in the normal/returning-user state, and to **S1** (sign-in + create/join) only while the "New user" dev switch simulates a fresh install — matching ux-plan's own framing of S1 as the onboarding screen. This is the one interpretive call in the ticket I'd most want the reviewer to check.
- **Recipe-detail content only has three worked examples** (chicken stir-fry / miso salmon / Thai noodles), matching the three verdict states. The fourth recipe row on the Recipes screen (yogurt berry parfait) does not open a fourth detail screen — tapping it opens a toast explaining the reuse rather than showing mismatched content (wrong photo/title against real ingredients). Building a fourth full detail screen was judged not worth the added file size against the ticket's actual acceptance bar (three verdict states, not "every card has bespoke detail content").
- **Numeric continuity.** The chicken-breast numbers (2.0 lb bought, 0.75 lb used, 1.25 lb on hand) are reused verbatim across S4 (inventory row), S5 (ledger + clamp context), Shopping (gap math), and the recipe-detail shortfall case, tying the brief's own worked example (§7) through the whole prototype. The recipe-detail ingredient row states "need 2 lb" for tonight's stir-fry (a doubled batch, to justify the shortfall demonstration the ticket explicitly asks for) even though the compact card badge elsewhere just shows a plain "have" checkmark — the compact badge is a binary summary, the detail screen is the precise figure; not a contradiction, but a design choice, flagged for the reviewer.
- **Dev switches are additive, not mutually exclusive.** "Camera denied" and "Recipes: none/blocked" etc. can technically be combined with other switches in ways a real app would never allow simultaneously (e.g. camera denied while the no-match toggle is also active). Acceptable for a review tool; not shippable behaviour.
- **44px tap targets.** This is a pre-existing gap in the adopted v3 design system, not something introduced here: `.chip`, `.loc`, `.seg button` and my new `.sevbtn`/`.allergychip` all sit around 34–36px tall, following the established visual language exactly. A full audit/resize of every chip-style control across the whole prototype (existing screens included) was out of this ticket's scope; flagging it as a backlog item below rather than silently claiming full P9 compliance.
- **Back-button accessibility labels.** Existing `.iconb` back buttons (all screens, pre-existing pattern) have no `aria-label`. New screens follow the same existing pattern for visual consistency rather than being singled out for a fix that the rest of the file doesn't have.

## 9. Honest gaps / not done

- Dark mode (explicitly out of scope, OQ-D6).
- Contrast-ratio measurement (explicitly out of scope per the epic; M3-E0-T3's job).
- The copy deck cross-check (M3-E0-T2, running in parallel) — see §7.
- Per-field label-data tier granularity beyond what already existed at D-019 adoption (OQ-D7, unchanged).
- 44px tap target audit across chip-style controls (see §8) — proposing this as a backlog item, not silently fixing the whole design system under this ticket's scope.
- No screenshots were committed (per instructions); visual review is via the file itself at 390×844 in a browser.

## 10. Proposed backlog items (for the architect to file, not filed by this worker)

1. Full 44px minimum tap-target audit across all chip/segmented-control components (`.chip`, `.loc`, `.seg button`, `.sevbtn`, `.allergychip`), likely a design-direction token change, not a one-screen fix.
2. `aria-label`s on every `.iconb` back-button and icon-only control, repo-wide.
3. Resolve OQ-1 (shortfall record-and-flag policy) to DECIDED so the recipe-detail cook-confirm copy in §7 item 4 can be finalized rather than provisional.
4. A fourth recipe-detail worked example (or a data-driven template) if product wants every recipe card to open bespoke detail content rather than three canonical examples.

## 11. Format / lint

`npx prettier --check` on the two changed files: pass. Repo-wide `pnpm format:check` fails only on an unrelated untracked agent-harness file outside this ticket's scope (see §4). No lint step applies to `docs/**` (not part of the lint config's source globs).

## 12. Fixes applied (post-review)

Independent Opus review of commit `585890c` returned **PASS WITH FIXES** (F1-F9). All nine required findings plus the two "if trivial" items were applied in one follow-up commit, same file scope (`docs/design/mockups/smart-kitchen-prototype.html`, `docs/design/ux-plan.md` §7 only, this report).

| # | Finding | Before | After | Evidence |
|---|---|---|---|---|
| F1 | S2 "Skip" bypassed the allergy gate | `<a onclick="go(dev.newUser?'scr-home':'scr-profile')">Skip</a>` | `<a onclick="skipPreferences()">Skip preferences</a>`; `skipPreferences()` clears `#prefChips .chip.on` then calls `continueAllergies()`, so the none/any check still fires | `grep -n "skipPreferences"` shows the link and the function; `continueAllergies()` unchanged, still gates on `noneBtn`/`selectedAllergens` |
| F2 | S5 ledger did not reconcile (+2.0 − 0.75 − 0.75 = 0.5 ≠ 1.25 header) and the clamp row had the wrong sign (clamp shown as `−0.75` instead of an add-back) | 3 rows: `+2.0`, `−0.75` (stir-fry), `−0.75` (labelled "System correction") | 4 rows, restaged: `+2.0 lb` (Wed, purchase) · `−2.25 lb` (Thu, "Used in Chicken & spinach stir-fry", full-magnitude over-record) · `+0.25 lb` **Adjustment · corrects the 7:20 pm entry** (system, tier `≈ Est.`, text: "You recorded 2.25 lb but our record showed 2.0 lb. We added back the 0.25 lb difference so the count matches reality. Not entered by a person.") · `+1.25 lb` (Sat, purchase, new row). Arithmetic: 2.0 − 2.25 + 0.25 + 1.25 = **1.25 lb**, matching the header, the Lots row ("1.25 lb bought Sat, expires in 2 days" — unchanged), the Inventory row, the Shopping gap, and the recipe-detail ingredient row. The "why" line was rewritten to state this arithmetic explicitly. | `sed -n '944,995p' docs/design/mockups/smart-kitchen-prototype.html`; hand-verified 2.0−2.25+0.25+1.25=1.25 |
| F3 | "Correct this" and the reason chips only toasted, they did not touch the ledger | `correctQty()`/`applyReason()` called `toast(...)` only | `correctQty()` computes `delta = itemQty − itemBaseline` and **prepends** a new `Adjustment · ±X lb · DC · ✓ Fact · just now` row to `#historyList` (top), updates the qtybig header and the why-line, then toasts with Undo. `applyReason()` **appends** a `Consumed/Discarded/Marked expired/Donated · −X lb · reason: … · ✓ Fact · just now` row before `#ledgerAppendPoint` (bottom), zeroes the on-hand quantity, then navigates. New helpers: `ledgerRowHtml()`, `prependHistoryRow()`, `appendHistoryRow()`, `updateWhyPlain()`. | `grep -n "function correctQty\|function applyReason\|prependHistoryRow\|appendHistoryRow"` |
| F4 | BLOCKED surfaces were missing the standing caveat, and the all-blocked banner's two rows had no matched-term evidence | `.verdictblock.blocked` had evidence but no caveat; all-blocked rows had only "Contains X · blocked for Maya", no evidence line, no caveat under the banner | Added "Known matches only · not a guarantee this food is safe." under `.verdictblock.blocked` and under the all-blocked banner; added `Matched "peanuts" in the ingredient statement (label data).` and `Matched "cashews" in the ingredient statement (label data).` under both all-blocked rows | `sed -n '739,756p'` and `sed -n '1053,1066p'` |
| F5 | The cashew all-blocked row opened the Thai peanut noodles detail screen (wrong content on tap) | `onclick="openRecipe('blocked')"` | `onclick="toast('Recipe detail reuses the Thai peanut noodles example in this prototype for the BLOCKED state')"` — non-navigating, honest toast, same pattern already used for the Yogurt parfait row | `grep -n "Creamy cashew curry" -A1` |
| F6 | No marker that the shortfall record-and-flag policy is still OQ-1/PROPOSED, not DECIDED | none | Added `"How shortfalls are handled is still being decided (OQ-1). This screen shows the proposed policy."` to both `#cookSheetOk` and `#cookSheetUnk` | `grep -n "OQ-1"` → 2 hits, both cook sheets |
| F7 | `.scanmode` match/no-match buttons lived inside the phone frame in terracotta, so the default scan screen did not equal adopted v3 | in-screen `<div class="scanmode">…</div>` with `.scanmode button.on { background: var(--brand) }` | Removed from the screen and its CSS entirely; added `<button class="dsw" id="dswScan" onclick="toggleScanMatch()">Scan: match</button>` to `.devbar` (same `.dsw`/`.dsw.on` espresso pattern as the other dev switches); `toggleScanMatch()` flips `dev.scanMatch` and `applyDev()` drives `#scr-scan.no-match`, the camera hint text and the dev-switch label | `grep -n "scanmode"` → 0 hits; `grep -n "dswScan\|toggleScanMatch"` |
| F8 | Remaining terracotta scarcity removals | see table below | see table below | see table below |
| F9 | Missing tier chips on new fallible values | "Used in stir-fry" row had no chip; clamp row had no chip; Miso/Broccoli "missing" rows had no chip; `.allocrow` amounts had no chip | Added `✓ Fact` to the stir-fry row; `≈ Est.` to the Adjustment/clamp row (clamps are engine-derived, not a verified fact); `✓ Fact` to both "missing" ingredient rows (absence is itself a ledger fact); a tier chip on every `.allocrow` (chicken/spinach `✓ Fact`, mushrooms `AI`, rice `≈ Est.`, salmon `✓ Fact`) | `grep -n "prov fact\|prov est\|prov ai" ` around lines 986-1050 |

Trivial items also applied:
- Yogurt berry parfait row now carries the same ALLOWED verdict line + caveat as its siblings, so its absence no longer reads as an implicit clearance.
- `docs/design/ux-plan.md` §7 S4 row changed from `—` to an explicit open gap: "stale-offline state still not designed (open gap, not done)".

### F8 — final terracotta (`--brand`/`--brand-deep`/`--brand-tint`) inventory

| Use | Action | Rationale |
|---|---|---|
| `.btn.pri` / `.btn.pri:active` | **kept** | Primary CTA — explicitly allowed |
| `.nav button.on` (+ its svg stroke) | **kept** | Active nav indicator — explicitly allowed |
| `.hero h1 em` (italic "tonight") | **kept** | Hero italic accent — explicitly allowed |
| Sparkle icon in `.ask` (`stroke="#f4b48f"`) | **kept** | Grouped with the hero accent under "sparkle" in the review's keep-list |
| `.frame i`, `.laser` (scanner brackets/sweep) | **kept** | Explicit exception named in the review; recorded here as instructed |
| `.loop b` (banner text) | **kept** | "Brand text only" — explicitly allowed; the loop banner's background and button were already de-branded in the first pass |
| `.tag.brand` | **deleted** | Dead rule, no longer referenced anywhere (both usages were replaced with `.tag.glass` in the first pass) |
| `.sec a` (section links: "All matches", "Invite", legend link, etc.) | **changed** `--brand-deep` → `--ink-2` | F8 |
| `.leglink` ("What do these badges mean?") | **changed** `--brand-deep` → `--ink`, added an underline so the link affordance is not colour-only | F8 |
| `.pill .dot` (Home hero, "shopping gaps" stat) | **changed** inline `var(--brand)` → `var(--ink-3)` | F8. Note: this dot sits on the dark hero, where `--ink-3` was designed as a light-surface tone, so its contrast against espresso is low; it is a decorative dot beside a number and label that already carry the meaning, not a sole signal, but flagged here in case the reviewer wants a hero-specific muted token instead |
| `.scanmode button.on` | **removed** (whole control moved to `.devbar`, see F7) | F7/F8 |
| `.ask .go` (hero "Ask your kitchen" go-button) | **changed** to the `.hbtn` translucent-cream treatment | F8 — post-MVP affordance must not wear the CTA colour |
| `.send` (Assistant chat send-button) | **changed** to `var(--espresso)` | Not explicitly named in F8's list, but the same screen and the same "post-MVP affordance must not wear the CTA colour" rationale as `.ask .go` applies directly to it (it is the send button inside the post-MVP assistant preview itself); fixed for consistency and disclosed here since it was not an explicit finding |
| `.loc.on` (location-chip selected state, scan-confirm + manual add) | **changed** to the `.chip.on` espresso/cream pattern | F8 — brand is for focus, not selection |
| `--brand-tint` custom property | **now unused** in this file (both its former consumers, `.loop` and `.loc.on`, were changed) | Left defined in `:root` since it is part of the documented design-direction §0 token sheet, not this ticket's to remove; flagged for M3-E0-T3 |

### Verification after fixes

| Check | Result |
|---|---|
| `grep -c "—"` | **0** |
| `grep -noi "safe\|passed\|allergen-free\|guaranteed"` | 13 matches, all negated/explanatory (re-verified line by line; no affirmative safety claim) |
| `grep -n "modeMatch\|modeMiss"` (F7 leftover check) | 0 hits |
| `grep -n "tag brand\|tag\.brand"` | 0 hits (dead rule deleted, no stray usages) |
| HTML tag balance (scripted open/close walk) | 0 mismatches, 0 unclosed |
| JS syntax (`new Function(scriptBody)`) | parses clean |
| `npx prettier@3 --check docs/design/mockups/smart-kitchen-prototype.html docs/design/ux-plan.md` | pass |

### Still flagged, not fixed (carried over from §6-8, unchanged by this pass)
- The S1-vs-S12 avatar-routing interpretation.
- New copy has still not been cross-checked against the sibling M3-E0-T2 copy deck.
- The 44px tap-target shortfall is systemic to the adopted v3 design system; not touched here.
- `.pill .dot`'s contrast on the dark hero (new, see F8 table above) — genuinely uncertain whether `--ink-3` was the intended token for a dark surface; asking rather than guessing a different value.
