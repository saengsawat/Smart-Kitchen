# M1-T6 — Allergen screening: malformed allergen containers fail closed — review report

**Reviewer:** independent Opus session (read-only), dispatched by the architect (CLAUDE.md rules 22, 28–29).
**Branch/commit reviewed:** `m1-t6-allergen-malformed` @ `2289207` (base `main` @ `4eaad2f`). CI run 34921147810: **success** (verified by the architect via the public Actions API at acceptance; the reviewer had no `gh`).
**Outcome:** **PASS** — no fixes required. Squash-merged to `main` as `1fa171c` (architect, 2026-09-14).

## Verdict summary
The reviewer could not construct a single input in which a locus carrying a present-but-malformed `allergens` field reaches `ALLOWED` under a valid sourced `KNOWN_FACT` complete declaration. 488 hand-built probes (26 malformed containers + 31 malformed elements + 4 controls × 2 loci × 4 restriction kinds) plus 4 500 fast-check runs: **zero `ALLOWED` leaks, zero over-corrections** (absent / `[]` still `ALLOWED` with zero unknowns), and **`BLOCKED` is never weakened** (valid `CONTAINS` beside junk, alias folding, severe `MAY_CONTAIN` beside junk, and text-term matches all still block). The worker's mutation table reproduced exactly (62 / 50 / 111). The one modified pre-existing assertion is a genuine strengthening; the dedicated INV-ALRG-1/2 suites are untouched. All gates green. Findings are all LOW/INFO, pre-existing or out of scope — none required before acceptance.

## Findings
- **F1 — LOW (pre-existing, out of scope):** `screen.ts` `parseAssertions` reads `input.allergenCode` twice (`typeof` guard then value); a getter returning a string first and a number second escapes the guard and `screenSubject` throws (`raw.normalize is not a function`). Identical lines exist at `4eaad2f`. Fails loud, not open. Backlog: single-read guards (two lines).
- **F2 — LOW (pre-existing class, worker-disclosed):** an element accessor that throws propagates out of `screenSubject`. Same class as the worker's §10 subject-level note; hostile object graph in the caller's own memory. No action.
- **F3 — INFO:** a `Proxy` over an array lying `length = 0` over a `CONTAINS` yields `ALLOWED` (`Array.isArray` is true; a list reporting length 0 is indistinguishable from `[]`). Unreachable from JSON/DB/adapter payloads. No action.
- **F4 — INFO (pre-existing, adapter-boundary):** a *misplaced* recipe-level `allergens` field is silently ignored (`buildLoci` reads only `ingredient.allergens`). Same family as the worker's backlog item 4 (port-boundary rejection); fold into that M4 item.
- **F5 — INFO:** mutation M-2 kill count depends on the mutant's exact guard shape (51 vs 50); the exact pre-M1-T6 guard reproduces the worker's 50. No action.
- **F6 — INFO:** `[undefined]` and sparse holes fail closed but only the totality battery exercises them; no test pins their `detail` string. Optional hardening.
- **F7 — INFO:** a member with zero restrictions + malformed data yields `ALLOWED` carrying `UNRECOGNIZED_ALLERGEN_DATA` — consistent with pre-existing semantics; flag to the M3/M6 copy layer that `ALLOWED` can co-occur with that warning.

## Adversarial probes executed (summary)
Every subject would have been `ALLOWED` had the malformation been ignored (impeccable sourced `KNOWN_FACT` declaration, no allergen term in text).
- **Containers (26 shapes × 8 = 208 probes, 0 `ALLOWED`):** `null`, `""`, `"peanut"`, `0`, `NaN`, `false`, `true`, `1n`, `Symbol()`, `{}`, array-like `{length:1,0:{…}}`, `Object.create(null)` (empty and array-like), `JSON.parse('{"__proto__":…}')`, functions, `Date`, `Map`, `Set`, `RegExp`, `Int8Array`, `Promise`, `new String("peanut")`, generator, `{toJSON}`, JSON string of a valid array → all `ALLOWED_WITH_UNKNOWNS` / `MALFORMED_ALLERGEN_DATA`.
- **Elements (31 shapes × 8 = 248 probes, 0 `ALLOWED`):** `["peanut"]`, `[null]`, `[undefined]`, `[,]`, `new Array(3)`, `[5]`, `[NaN]`, `[false]`, `[0]`, `[""]`, `[Symbol()]`, `[1n]`, `[fn]`, nested arrays, `[{}]`, `[Date]`, `[Map]`, null-prototype objects, non-string `allergenCode`/`assertion`, 1 000- and 100 000-element null arrays (33 ms) → all closed. `[contains(peanut), null]` in either order, alias/case/whitespace folding beside junk, severe `MAY_CONTAIN` + junk, prototype-inherited assertion, text-term + malformed container, recipe malformed-locus + clean `CONTAINS` locus → all **`BLOCKED`**. Recipe malformed at ingredient 2 of 3 → correct `locus.part`/`ingredientIndex`/`ref`.
- **Controls (32/32):** absent key, explicit `undefined`, `[]`, `[CONTAINS soy]` → `ALLOWED`, 0 unknowns, no `UNRECOGNIZED_ALLERGEN_DATA`.
- **Fuzz (scratch, importing the domain barrel):** 2 000-run totality fuzz over `fc.anything` (bigint/date/map/set/null-proto/typed-array/sparse) across `allergens`/`name`/`ingredientsText`/`declaration` × both loci × 1–3 members × 1–3 restrictions — 0 throws (caveat: `fc.anything` does not generate accessors, so F1/F2 were found by hand). 1 500-run never-`ALLOWED` over generated malformations — every member verdict ≠ `ALLOWED`, all unknowns `MALFORMED_ALLERGEN_DATA`, warning present, result deep-frozen. 1 000-run over-correction guard — absent/`[]` still `ALLOWED`.

## Mutation re-measurement (reviewer vs worker; allergen suite = 299)
| # | Mutation | Worker | Reviewer |
|---|---|---|---|
| M-1 | malformed container → empty list | 62 | **62** |
| M-2′ | malformed element → exact pre-M1-T6 guard + `continue` | 50 | **50** |
| M-2 | variant keeping `Array.isArray` in the guard | — | 51 (F5) |
| M-3 | both (pre-M1-T6 behaviour) | 111 | **111** |

## Acceptance criteria checklist — all MET
1. Every malformed shape, both loci, valid declaration ⇒ `ALLOWED_WITH_UNKNOWNS` + `MALFORMED_ALLERGEN_DATA` + shape-naming `detail` + `UNRECOGNIZED_ALLERGEN_DATA` (456 probes + repo's 88-case matrix; `detail` byte-exact).
2. Well-formed beside malformed still parsed and still blocks (both orders, both loci; M-3 leaves all 188 non-M1-T6 tests passing ⇒ no existing `BLOCKED` moved).
3. Totality for the malformed matrix (2 000-run fuzz + 488 probes, 0 throws; F1/F2 accessor throws pre-exist byte-identically).
4. Property test present (`invariants.property.test.ts`: 300 runs + 200-run over-correction mirror) and independently confirmed.
5. Mutation check reported and reproduced.
6. One new `UnknownReason` only; no new verdict/warning/public function; `index.ts` unchanged; deep-freeze intact.
7. Absent ≠ malformed (`null` correctly read as malformed).
8. Fixture `screening-016-malformed-allergens-not-allowed.json` deep-equal to its inline mirror; in `CASE_FILES`; README index line present.
9. INV-* tests untouched. Sole existing-test edit (`regressions.review.test.ts` F4: `NO_ALLERGEN_DATA` → `MALFORMED_ALLERGEN_DATA` + new `unknowns.length === 1`, verdict assertion unchanged) judged a **legitimate strengthening** — the old expectation encoded the fail-open reading this ticket removes; it now dies under M-1/M-3.

## Scope & dependency check
Nine files, all inside the declared scope; no `docs/**` edits beyond the worker report (rule 30 respected); `git diff 4eaad2f..2289207 -- package.json pnpm-lock.yaml '**/package.json'` empty (rule 11); no secrets, migrations, or external calls.

## Gate results (reviewer-run on the branch)
`pnpm lint` 0 warnings · `pnpm typecheck` exit 0 · `pnpm format:check` clean · `pnpm test` **33 files, 525 passed / 92 skipped** (DB suites skipped locally, loud banner) · allergen suite **299** incl. `malformed-allergens.test.ts` **133**. Every worker-reported number reproduced exactly.

## Reviewer opinions (labeled; architect disposition at acceptance in brackets)
- Worker doc update 1 (`domain-model.md` `MALFORMED_ALLERGEN_DATA` wording): agree, apply verbatim. **[Applied — AllergenAssertion row.]**
- Worker doc update 2 (`testing-strategy.md` §3 invalid-on-purpose corpus inputs): agree. **[Applied.]**
- Worker backlog 1 (warn-only raw-text scan of malformed entries): agree it is backlog and must never become a `MATCH`; lower priority than the adapter-side fix. **[Recorded, M4.]**
- Worker backlog 2 (only the first uninterpreted entry per locus reported; 100 000-null probe returns 1 unknown): agree, slightly more valuable than suggested — an M3 "what is unknown" surface under-reports. **[Recorded, own ticket pre-M3.]**
- Worker backlog 3 (recommendations README cites non-existent `fixtures.test.ts`): leave to M1-T10 item (i). **[Folded into M1-T10 (i).]**
- Worker backlog 4 + reviewer F4 (port-boundary rejection of malformed/misplaced `allergens`): agree, raise priority. **[Recorded, M4 mandatory alongside gate (b).]**
- Reviewer additions: F1 single-read guards (two lines); F6 detail-string rows for `[undefined]`/holes; F7 copy-rule note. **[Recorded: F1+F6 as a small pre-M4 allergen hardening ticket; F7 added to the M3/M6 copy requirements.]**
- D-017 untouched and correctly so; gate (b) remains open.
- Overall: "the strongest worker package I have reviewed on this repo — the report's self-criticism all checked out as stated." The M1-T4 review's mandatory pre-M4 gate (a) is **closed**.

## Confirmation
Repo restored to `2289207` before leaving the branch (`git diff --stat` and `--cached` empty); returned to `main`; only the two pre-existing untracked user files (`docs/architecture/workflow-diagrams.{md,html}`) present, untouched. Nothing committed, pushed, merged or written into the repo by the reviewer; probes live only in the reviewer's scratch directory.
