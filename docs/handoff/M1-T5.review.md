# M1-T5 — Product lookup ports + fixtures + R-1 research · independent review record (Sonnet)

**Branch reviewed:** `m1-t5-product-lookup` (initial @ `143572f`; doc-only fixes @ `2b14c4b`) · **Reviewer:** independent Sonnet agent (did not write the code), assisted by one fixture-verification sub-agent whose findings the reviewer independently corroborated · **Committed by the architect at acceptance per CLAUDE.md rule 29.**

## Verdict: PASS WITH FIXES → fixes applied (doc-only) → **accepted**

The shipped artifact — ports, fixture corpus, contract tests, research spike — passed every check by direct execution and inspection. All three required fixes were corrections to the worker report's own accounting, none to code, tests, fixtures, or research numbers.

## Verified by execution / inspection

- **Full gate from clean state:** install (frozen lockfile), lint, typecheck, 216/216 tests, format check, audit — all green. 150 pre-existing tests unmodified (zero touches to `packages/domain`, `packages/contracts`, `apps/**`). No `.skip/.only/.todo`. Sampled test files show real, non-tautological assertions.
- **Port-boundary hygiene:** no vendor types cross the port surface; `FieldProvenance {tier, source, observedAt, confidence?}` exact; the only I/O in the package is the fixture loader reading the two fixture paths; the live research script is structurally excluded from `pnpm test`/CI (vitest include globs) and imported nowhere; the only secret-like literal anywhere is the documented public `DEMO_KEY`.
- **Fixture corpus (101 synthetic products, 109-entry manifest):** category and case tallies independently recomputed — exact match on every claimed count; GS1 mod-10 check digits hand-verified on samples (incl. the UPC-A/GTIN-13 multi-code twin); **live Open Food Facts queries confirmed the synthetic codes resolve to "product not found"** — no real-product collisions; brands invented; ingredients text synthetic.
- **Research integrity (R-1):** every headline number recomputed from the raw JSON — exact match (OFF 102/110 raw; name arm 87/87; PLU arm 15/23; all field-completeness percentages). FDC honestly reported as **27 attempted / 0 completed, all HTTP 429** — no fabricated or extrapolated FDC numbers anywhere. Five live OFF spot-checks reproduced the recorded results, including the doc's specific named mismatches (PLU 4070 "Celery" → a banana tart; "Kirkland whole milk" → artichoke hearts). Basket-construction bias limitations disclosed up front; the mid-run endpoint bug (first draft silently mis-scoring an arm) disclosed in §8 and fully re-run. ODbL flagged for counsel (R-4).
- **Dependencies:** one devDependency (`@types/node`, same version as root, no new lockfile package, type-only) — sanctioned by the ticket's scope allowance.
- **File scope:** every changed file within the granted scope; CI runs 34313854631 / 34313926045 / 34314001582 green (public Actions API); fix commit `2b14c4b` (docs-only, verified by diff) green — run 34314709836 (architect-verified at acceptance).

## Findings (all LOW, report accuracy — rule 19; fixed in `2b14c4b`)
1. "Complete branded items: 78" didn't reconcile under any constructible definition (74 under the most plausible residual-bucket reading) — recomputed/relabeled with a stated definition.
2. Missing-nutrition and generic/PLU buckets overlap (6 of 8) — now disclosed.
3. Stale branch-head reference in §6 (written before the final docs-only commit existed) — superseded note added.

## UNVERIFIED (disclosed)
- Not every one of the 66 new test files read line-by-line (representative samples read in full; consistent quality).
- Not all 101 fixtures individually inspected for brand invention (2 direct reads + an 18-brand independent tally; all invented).
- The fixture-verification sub-agent's raw transcript wasn't seen by the reviewer directly (results relayed via the architect); its key claims were independently re-derived by the reviewer and matched exactly.

## Reviewer opinions (labeled, architect-ratified)
- **Port design: well executed** — Outcome-style totality consistent with the domain idiom; multi-source disagreement modeled as multiple provenanced profiles rather than a premature merge or a fourth status (merge is M4's job); real GS1 validation instead of length checks.
- **ADR-006 OPEN → PROPOSED: supported.** Evidence honestly measured with disclosed bias; the PLU finding (a wrong-but-"found" product ~60% of the time the PLU arm "hits" — worse than a clean miss) deserves promotion to a settled sub-decision; FDC correctly labeled *unvalidated, not disproven* (0/27 rate-limited). Endorsed the worker's proposed ADR wording essentially as written.
