// Placeholder — populated starting M1-T1 (inventory ledger core).
// This package must never gain a runtime dependency or import Node I/O
// modules; the dependency-boundary lint (eslint.config.js) enforces that.
export const DOMAIN_PACKAGE_NAME = "@smart-kitchen/domain";
