import { PlaceholderScreen } from "../src/screens/PlaceholderScreen";
import { CENTER_ACTION } from "../src/navigation/tabs";

export default function AddScreen(): React.JSX.Element {
  return <PlaceholderScreen title={CENTER_ACTION.label} subtitle={CENTER_ACTION.placeholder} />;
}
