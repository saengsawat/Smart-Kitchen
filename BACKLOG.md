# BACKLOG.md — Implementation Backlog

Authoritative work plan. Structure: **Milestone → Epic → Ticket**. Only M0 and M1 are fully ticketed (per plan discipline — no speculative ticket dumps); M2–M9 stay at epic level until their predecessor is underway. Sequencing is dependency-driven, not demo-driven.

> **Tracking view:** a spreadsheet mirror of this backlog (tickets, model recommendations, status column) lives in `BACKLOG_TRACKER.xlsx` / `BACKLOG_TRACKER.csv` for status tracking. **This file remains authoritative** for ticket content; when tickets change here, regenerate/update the tracker (status values live in the tracker only).

**Gate:** No implementation starts until the product owner approves D-002 (MVP scope) and D-003 (ledger) in [DECISIONS.md](DECISIONS.md). Ticket 1 additionally requires explicit go-ahead from the product owner (per founding task).

**Ticket template** — every ticket includes, in order:

1. **Implementation model:** Sonnet | Opus — reason
2. **Review model:** Sonnet | Opus — reason
3. **Objective**
4. **Context**
5. **Dependencies**
6. **Invariants**
7. **Acceptance criteria**
8. **Tests required**
9. **File scope**
10. **Out of scope**
11. **Definition of done (DoD)**

Model routing rules (default Sonnet; Opus for the high-risk domain list; separate reviewer always required before done) are defined in [CLAUDE.md rules 20–27](CLAUDE.md). The architect sets the model lines; deviating requires architect sign-off recorded on the ticket. Work outside a ticket's file scope requires escalation, not improvisation.

---

## Milestone 0 — Engineering foundation
*Goal: a repo where `git clone → install → test` works, CI enforces quality, and module boundaries exist before code fills them.*

### Epic M0-E1: Workspace & quality gates

#### M0-T1 — Repo & workspace scaffolding ✅ DONE 2026-09-03
*Sonnet impl → independent Sonnet review (PASS WITH FIXES: `.gitattributes`, release-age comment — applied & re-verified) → accepted & merged. ADR-002 framework question closed (Fastify) before dispatch.*
- **Implementation model:** Sonnet — routine tooling/config on well-trodden patterns; no domain logic, no data-integrity risk.
- **Review model:** Sonnet — low blast radius; review focuses on boundary-lint correctness and script reproducibility.
- **Objective:** pnpm monorepo with `apps/api`, `packages/domain`, `packages/contracts`, `packages/adapters` (placeholder packages, no product logic); TypeScript strict everywhere; ESLint + Prettier; Vitest wired; dependency-boundary lint rule proving `domain` imports nothing but stdlib.
- **Context:** ARCHITECTURE.md §2; ADR-002 (resolve Fastify-vs-Nest spike inside this ticket and record in ADR-002/DECISIONS).
- **Dependencies:** D-002/D-003 approval; Node LTS + pnpm locally.
- **Invariants:** `packages/domain` has zero runtime deps and zero I/O; lockfile committed; no secrets anywhere (`.env.example` names only).
- **Acceptance criteria:** fresh clone → `pnpm install && pnpm lint && pnpm typecheck && pnpm test` passes; a deliberate `domain → adapters` import fails lint.
- **Tests:** one placeholder unit test per package proving the harness runs.
- **File scope:** repo root configs, `apps/api/*`, `packages/*` skeletons, `.gitignore`, `.editorconfig`, `.env.example`.
- **Out of scope:** CI (M0-T2), any domain logic, DB, client app.
- **DoD:** CI-ready scripts documented in README dev section; ADR-002 framework question closed; STATUS.md updated.

#### M0-T2 — CI pipeline & quality gates ✅ DONE 2026-09-08
*Sonnet impl → independent Sonnet review (PASS, no fixes — all runs re-verified via live Actions API) → accepted & merged. Worker deviation (dropping `version:` input from pnpm/action-setup in favor of exact `packageManager` pinning) architect-approved: forced by action incompatibility, disclosed with run evidence, strictly tighter pinning. Branch protection = owner checklist in CONTRIBUTING.md; architect verifies the `protected` flag after the owner applies it.*
- **Implementation model:** Sonnet — standard CI configuration; security-adjacent (secret/dependency scanning) but assembled from stock actions, verified by deliberate-failure PRs.
- **Review model:** Sonnet — acceptance criteria are self-demonstrating (the three failing test PRs); reviewer checks gate coverage and branch protection.
- **Objective:** CI (GitHub Actions assumed once hosting of repo is settled — confirm) running lint, typecheck, unit tests, **format check** (now that `.gitattributes` normalizes line endings), dependency-boundary check, secret scan (gitleaks-class), dependency audit on every PR; trunk-based flow with short-lived branches; conventional commits with ticket refs documented; short CONTRIBUTING notes incl. Windows long-path caveat (`git config core.longpaths true` / Win32 long paths — M0-T1 follow-up) and pnpm/corepack setup.
- **Context:** ARCHITECTURE.md quality baseline; CLAUDE.md rules. Remote decided 2026-09-03: `github.com/saengsawat/Smart-Kitchen` (main) → GitHub Actions confirmed as CI host.
- **Dependencies:** M0-T1 (done); GitHub remote (done).
- **Invariants:** no merges to main with red CI; lockfile drift fails CI (`--frozen-lockfile`).
- **Acceptance criteria:** a branch/PR with a lint error, a leaked fake secret, or a domain-boundary violation each fail CI visibly (workflow triggers on both `push` and `pull_request`, so pushed demo branches prove the gates without PR tooling).
- **Tests:** CI exercised by three deliberate-failure demo branches, run conclusions verified via the repo's public Actions API, then demo branches deleted.
- **File scope:** `.github/workflows/*`, `CONTRIBUTING.md` (or README CONTRIBUTING section), README badge/link lines.
- **Out of scope:** deployment, E2E infra, coverage gates, installing new local tooling (incl. `gh` CLI — not installed; do not install without approval).
- **DoD:** CI green on main; badges/links in README; **branch-protection checklist delivered for the owner to apply in repo Settings** (no admin API access from this machine — architect verifies application at acceptance); STATUS.md updated (architect, on acceptance).
- *Architect amendment 2026-09-08:* verification method and branch-protection delivery adjusted for missing `gh` CLI; substance of gates unchanged.

---

## Milestone 1 — Domain core + inventory ledger
*Goal: the product's heart — ledger, units, allergen rules, gap math — as pure, property-tested TypeScript with no server, no DB dependency for logic, plus the first real schema. Proves ADR-008 in code.*

### Epic M1-E1: Deterministic domain package

#### M1-T1 — Inventory ledger core ✅ DONE 2026-09-08
*Opus worker → independent Opus review (PASS WITH FIXES: forged-systemFlag spread + rehydration input-trust — fixed by worker, re-reviewed, final PASS; fixes mutation-proven) → squash-merged (keeps a gitleaks false-positive fixture commit out of main history). 75 tests incl. INV-LEDGER-1..4 property suites. Handoff: [worker](docs/handoff/M1-T1.worker.md) · [review](docs/handoff/M1-T1.review.md). Clamp/idempotency semantics recorded under D-003.*
- **Implementation model:** **Opus** — the highest-risk domain work in the system: ledger/reconciliation math, idempotency semantics, immutability guarantees (CLAUDE.md rule-23 list, multiple hits).
- **Review model:** **Opus** — invariants INV-LEDGER-1..4 are permanent product guarantees; review must adversarially probe derivation, replay, and negative-quantity edge cases.
- **Objective:** `packages/domain/inventory`: transaction types (`PURCHASE|CONSUME|USE_IN_MEAL|DISCARD|EXPIRE|DONATE|ADJUSTMENT|INITIAL_STOCK`), lot/item aggregates, quantity derivation, idempotency-key semantics, negative-quantity policy (implement the clamp+flagged-adjustment proposal; record outcome in domain-model OQ).
- **Context:** [domain-model.md](docs/architecture/domain-model.md), [ADR-008](docs/adr/ADR-008-inventory-ledger.md), brief §7.
- **Dependencies:** M0-T1.
- **Invariants:** INV-LEDGER-1..4 ([testing-strategy.md §2](docs/architecture/testing-strategy.md#2-named-invariant--property-tests)).
- **Acceptance criteria:** derivation over any generated transaction sequence reconciles; replay with same key is a no-op; immutability enforced by types + runtime guard.
- **Tests:** property tests (fast-check) for INV-LEDGER-1..4; example-based tests for each transaction type incl. the brief's worked example (2.0 − 0.75 = 1.25 lb chicken, §7).
- **File scope:** `packages/domain/src/inventory/**`, its tests.
- **Out of scope:** persistence, API, units beyond pass-through (M1-T3).
- **DoD:** invariant tests named with their IDs; domain-model.md updated where implementation refined the model.

#### M1-T2 — Postgres schema & migrations: identity, household, inventory ✅ DONE 2026-09-10 — **MILESTONE 1 COMPLETE**
*Opus worker → adversarial Opus review (PASS WITH FIXES: INV-LEDGER-4 failed OPEN via RLS-filtered non-definer trigger; ledger-length oracle via unscoped unique index — both closed against the reviewer's original repros; worker also self-found a CREATE ROLE CI race) → final PASS → squash-merged. 96 DB tests (475 total w/ DB). ADR-003 → DECIDED (Postgres + RLS, three standing schema rules); ADR-008 snapshot question closed (DB trigger). Handoff: [worker](docs/handoff/M1-T2.worker.md) · [review](docs/handoff/M1-T2.review.md).*
- **Implementation model:** **Opus** — sensitive schema for inventory history, append-only enforcement, tenancy isolation/RLS spike, snapshot-consistency decision (rule-23 list: authorization + schema migrations affecting inventory history).
- **Review model:** **Opus** — isolation and append-only guarantees must survive adversarial review; migration reversibility affects everything downstream.
- **Objective:** first migrations for users/households/memberships + inventory items/lots/transactions per [data-model.md](docs/architecture/data-model.md); migration tooling chosen (with ORM/query-layer decision from ADR-002 open question); append-only enforcement; snapshot-maintenance approach decided (app-tx vs trigger) and recorded; RLS spike → decision recorded in ADR-003/DECISIONS.
- **Context:** data-model.md §2–5; runs against real Postgres in CI — **no cloud resources**.
- *Architect amendment 2026-09-10 (Docker not installed on the dev machine):* DB tests key off `DATABASE_URL` and **skip loudly** when it is absent locally; `.github/workflows/ci.yml` gains a Postgres **service container** for the `quality` job so the tests always run and gate in CI (ci.yml is therefore IN scope for this ticket, narrowly). Local containerized Postgres becomes possible once Docker is installed (owner's call, CONTRIBUTING note).
- **Dependencies:** M1-T1 (types inform columns); D-007 ratified enough to proceed (PROPOSED→DECIDED expected here).
- **Invariants:** INV-TENANT-1 test harness exists (even with only fixture users); ledger tables reject UPDATE/DELETE from app role.
- **Acceptance criteria:** migrate up/down clean on empty + seeded DB; reconciliation query matches domain derivation on generated data.
- **Acceptance additions from M1-T1 (architect, 2026-09-08):** unique idempotency index scoped `(household_id, idempotency_key)` (ruling on M1-T1 §8.1 — batch imports suffix per line); snapshots updated in the same DB transaction as the append; reconciliation query must agree with domain `reconcile()` on generated data; `rehydrateInventoryItem` is the reference corruption detector for rows read back; store both `qty_delta` and exact `qty_delta_micros`.
- **Tests:** database tests per testing-strategy §1 (constraints, append-only, isolation), migration reversibility.
- **File scope:** `apps/api/db/**` (migrations), `packages/domain` untouched except type exports.
- **Out of scope:** HTTP endpoints (M2), auth integration (M2), receipt/shopping tables (later migrations).
- **DoD:** ADR-003 marked DECIDED (with evidence) or blockers recorded; RLS decision logged.

#### M1-T3 — Units & quantity model ✅ DONE 2026-09-09
*Sonnet worker → independent Opus review (PASS WITH FIXES: unpinned volume anchor — imperial-gallon swap passed the whole suite; makeBridge throw on exponential ratios; alias leak; non-antisymmetric comparator — fixed, re-reviewed, final PASS with mutations re-run) → squash-merged. 150 tests; factor table ground-truth-pinned. Handoff: [worker](docs/handoff/M1-T3.worker.md) · [review](docs/handoff/M1-T3.review.md). OQ-2 resolved in domain-model.md.*
- **Implementation model:** Sonnet — pure, well-specified conversion functions with strong property tests; errors are loud (typed `IncompatibleUnits`), not silent.
- **Review model:** **Opus** — escalated despite routine implementation: INV-SHOP-1 and future nutrition math sit on top of this; silent conversion errors would corrupt inventory arithmetic (rule-23 adjacency).
- **Objective:** `packages/domain/units`: mass/volume/count kinds, conversion within kind, product-specific cross-kind conversion only where data exists, graceful `IncompatibleUnits` failures (never guessed densities).
- **Context:** brief §6 examples mix lbs/cups/tbsp; INV-SHOP-1 depends on this; domain-model OQ-2.
- **Dependencies:** M0-T1.
- **Invariants:** conversions round-trip within tolerance; incompatible conversions are typed errors, not numbers.
- **Acceptance criteria:** brief §6 worked example computes exactly (chicken 2−1=1 lb; rice covered; broccoli 2−0=2 lb).
- **Tests:** property tests (round-trip, associativity of scaling), exhaustive kind-pair matrix.
- **File scope:** `packages/domain/src/units/**` + tests.
- **Out of scope:** localization of display units.
- **DoD:** OQ-2 resolved or narrowed in domain-model.md.

#### M1-T4 — Allergen rule engine (deterministic) ✅ DONE 2026-09-10
*Opus worker (restarted post-reboot) → adversarial Opus review (PASS WITH FIXES: three fail-open paths to ALLOWED — prototype-key code lookup, name-satisfied completeness gate, zero-width obfuscation — plus totality and alias-coverage gaps; fixed, re-reviewed against original repros, final PASS) → squash-merged. 163 allergen tests (379 total), mutation-pinned. Policies ratified as **D-017**; UI copy requirements recorded on M3/M6 epics. Handoff: [worker](docs/handoff/M1-T4.worker.md) · [review](docs/handoff/M1-T4.review.md).*
- **Implementation model:** **Opus** — allergen enforcement is explicitly on the rule-23 high-risk list; safety-critical semantics (`allowed-with-unknowns`, never "safe") must be exactly right.
- **Review model:** **Opus** — adversarial review required (INV-ALRG-1/2); a reviewer must actively try to construct inputs that slip past the screen.
- **Objective:** `packages/domain/allergens`: allergen taxonomy (FDA major allergens + user-defined), `AllergenAssertion` evaluation, recipe-vs-restrictions screening returning `{blocked | allowed-with-unknowns | allowed}` — *never* a plain "safe".
- **Context:** brief §3, §18E; SR-1/SR-2; [ai-architecture.md §2](docs/architecture/ai-architecture.md#2-capability-by-capability-boundaries).
- **Dependencies:** M0-T1; fixture products with allergen data (M1-T5 fixtures usable early).
- **Invariants:** INV-ALRG-1, INV-ALRG-2.
- **Acceptance criteria:** adversarial fixture (recipe containing peanut against peanut allergy) blocked regardless of any other field; missing data yields `allowed-with-unknowns` + warning payload.
- **Tests:** unit + adversarial fixtures from `tests/fixtures/recommendations/`.
- **File scope:** `packages/domain/src/allergens/**` + tests + `tests/fixtures/products|recommendations` additions.
- **Out of scope:** LLM integration, UI copy (copy requirements noted for M3/M6).
- **DoD:** screening API documented; safety language requirements captured for client tickets.

### Epic M1-E2: Product knowledge foundations

#### M1-T5 — Product lookup port + fixture adapter + coverage research spike ✅ DONE 2026-09-09
*Sonnet worker → independent Sonnet review (PASS WITH FIXES: three report-accounting corrections, doc-only; research numbers recomputed exact from raw JSON, live OFF spot-checks reproduced, synthetic codes live-verified non-colliding) → squash-merged. 216 tests. R-1 delivered → ADR-006 OPEN → PROPOSED (PLU sub-decision settled; FDC arm honestly unmeasured). Handoff: [worker](docs/handoff/M1-T5.worker.md) · [review](docs/handoff/M1-T5.review.md) · [research](docs/research/food-data-coverage.md).*
- **Implementation model:** Sonnet — adapter interfaces, fixture plumbing, and a read-only research script; no domain invariants at stake.
- **Review model:** Sonnet — review checks port-boundary hygiene (no vendor types leaking, provenance present) and that the research method/numbers in the spike doc hold up.
- **Objective:** `ProductLookupPort`/`NutritionSourcePort` interfaces in `packages/adapters`; fixture-backed implementations over `tests/fixtures/products|barcodes` (~100 curated synthetic products per testing-strategy §3); **research spike:** measure Open Food Facts + USDA FDC hit-rate on a realistic ~100-item US grocery basket (read-only API calls, free tiers, no keys committed) and write results to `docs/research/food-data-coverage.md`.
- **Context:** [ADR-006](docs/adr/ADR-006-food-data-sources.md) is OPEN pending exactly this evidence (R-1); brief §18A.
- **Dependencies:** M0-T1.
- **Invariants:** no vendor SDK types leak through the port; per-field provenance on every returned record.
- **Acceptance criteria:** fixture adapter serves hit/miss/conflict cases; spike doc reports hit-rates + data-quality notes + ODbL/licensing questions for counsel (R-4).
- **Tests:** contract tests over fixtures; adapter schema-validation tests.
- **File scope:** `packages/adapters/src/product-lookup/**`, `tests/fixtures/products|barcodes/**`, `docs/research/food-data-coverage.md`.
- **Out of scope:** live-source caching layer, our-catalog merge logic (M4), any paid API signup (requires approval per CLAUDE.md).
- **DoD:** ADR-006 updated from OPEN → PROPOSED with evidence, or gap documented.

### Epic M1-E3: Foundation follow-ups (PO-approved 2026-09-14 — D-018)
*Scope rule for this epic: only work that needs **no spend and no PO/originator decision** (D-018). Everything here was already accepted as a follow-up at an M1 acceptance; these tickets make them buildable. Order: T6 → T7 → T8 → T9 → T10, one at a time, architect-dispatched (rules 28–30). PO has asked for a **stop after T10 is accepted**.*

#### M1-T6 — Allergen screening: malformed allergen containers fail closed (mandatory pre-M4 gate a) ✅ DONE 2026-09-14
*Opus worker → independent adversarial Opus review (**PASS**, no fixes: 488 hand-built probes + 4 500 fuzz runs, zero `ALLOWED` leaks, zero over-corrections, `BLOCKED` never weakened; mutation table 62/50/111 reproduced exactly) → squash-merged `1fa171c`. +136 tests (525 w/o DB). Pre-M4 gate (a) **closed**. Handoff: [worker](docs/handoff/M1-T6.worker.md) · [review](docs/handoff/M1-T6.review.md).*
- **Implementation model:** **Opus** — allergen enforcement (rule-23 list); the change is small but it closes a fail-open path to `ALLOWED`, and three such paths were found in M1-T4 review.
- **Review model:** **Opus** — adversarial: the reviewer must try to construct a malformed input that still reaches `ALLOWED` under a valid declaration (INV-ALRG-2).
- **Objective:** `parseAssertions`/`asAssertionList` in `packages/domain/src/allergens/screen.ts` currently coerce a structurally malformed `allergens` field (`"peanut"`, `5`, `{}`, `["peanut"]`, `[null]`) to "no assertions", so under a valid sourced `KNOWN_FACT` declaration the locus reaches `ALLOWED`. Malformed containers and malformed elements must instead be recorded as **uninterpreted** data (new `UnknownReason` `MALFORMED_ALLERGEN_DATA`), which already refuses the absence licence (`licensesAbsence` returns false on any uninterpreted entry) and raises `UNRECOGNIZED_ALLERGEN_DATA`.
- **Context:** [M1-T4 review §"Mandatory pre-M4 follow-up"](docs/handoff/M1-T4.review.md); D-017; SR-2; rule 9 (data may add unknowns, never clear them).
- **Dependencies:** M1-T4 (done).
- **Invariants:** INV-ALRG-1, INV-ALRG-2. **Distinguish absent from malformed:** `allergens` missing/`undefined` and `allergens: []` remain "no assertions" (they are honest); only a present-but-wrongly-shaped container or element is malformed. `BLOCKED` results must be unchanged everywhere (this fix can only move `ALLOWED` → `ALLOWED_WITH_UNKNOWNS`).
- **Acceptance criteria:** (1) for every malformed shape above, on both `PRODUCT` and `RECIPE_INGREDIENT` loci, with a valid sourced `KNOWN_FACT` complete declaration, the verdict is `ALLOWED_WITH_UNKNOWNS` with a `ScreeningUnknown` whose `reason` is `MALFORMED_ALLERGEN_DATA` and whose `detail` names the offending shape, plus the `UNRECOGNIZED_ALLERGEN_DATA` warning; (2) a well-formed array element alongside a malformed one is still parsed (a `CONTAINS peanut` next to a `null` still blocks a peanut allergy); (3) `screenSubject` stays total — zero throws across the malformed matrix; (4) a property test: for any generated subject carrying a malformed container, verdict ≠ `ALLOWED` for every member with ≥1 restriction; (5) mutation check reported: reverting the fix fails ≥1 test per malformed shape.
- **Tests required:** unit matrix (shapes × locus kinds × restriction kinds), the property test, the mutation table in the worker report; one new recommendations fixture `screening-016-malformed-allergens-not-allowed.json` in `tests/fixtures/recommendations/` **and** its inline mirror in the domain corpus test (domain has no I/O).
- **File scope:** `packages/domain/src/allergens/{screen,types}.ts`, `packages/domain/src/allergens/*.test.ts`, `tests/fixtures/recommendations/**` (+ its README index line), `docs/handoff/M1-T6.worker.md`.
- **Out of scope:** declaration minting policy (D-017 open item — PO decision, pre-M4 gate b); taxonomy changes; adapters.
- **DoD:** rule 26; worker report lists the mutation results and proposes the `UnknownReason` doc line for `domain-model.md` §2 (architect applies at acceptance).

#### M1-T7 — Correction-rate telemetry: SQL view + read function (north-star KPI) ✅ DONE 2026-09-14
*Sonnet worker → independent Opus review (**PASS**, no fixes: 16 runtime tenancy probes on a real Postgres 17 cluster, 9-mutation battery — dropping `security_invoker` fails 4 tests; metric verified column-by-column incl. sum-then-divide household aggregate) → squash-merged `cf301c6`. 625 tests with DB / 527 without. Metric definition recorded in ARCHITECTURE §8. Handoff: [worker](docs/handoff/M1-T7.worker.md) · [review](docs/handoff/M1-T7.review.md).*
- **Implementation model:** Sonnet — a read-only view and a repository read; the metric definition is fixed below, so no domain judgement is needed.
- **Review model:** **Opus** — the view sits on tenant tables (ADR-003 standing rule 3: `security_invoker` or it leaks across households — tenancy is on the rule-23 list) and the KPI must count exactly the rows the definition says.
- **Objective:** migration `0007_correction_telemetry.sql` creating view `inventory_correction_telemetry` (`WITH (security_invoker = true)`) over `inventory_transactions`, one row per `(household_id, item_id)`, with columns: `statement_count` (rows that are **not** system clamp rows), `user_adjustment_count` (`type = 'ADJUSTMENT' AND actor_kind = 'user'`), `clamp_count` (`system_flag_kind = 'OVER_CONSUMPTION'`), `correction_event_count` (= user adjustments + clamps), `correction_rate` (`correction_event_count::numeric / statement_count`, `NULL` when `statement_count = 0`), `first_recorded_at`, `last_recorded_at`. Plus `apps/api/src/db/telemetry/correction-rate.ts` exporting `readCorrectionTelemetry(client, householdId, {since?, until?})` — household-level aggregate and per-item rows, computed in SQL over `recorded_at` bounds (`since` inclusive, `until` exclusive), returning exact strings/bigint counts (no float arithmetic on counts; rate as Postgres `numeric` text).
- **Context:** brief §18C (correction rate is the product's north-star metric); [M1-T1 review](docs/handoff/M1-T1.review.md) ("makes the correction-rate KPI a direct query"); [M1-T2 worker §11 item 3](docs/handoff/M1-T2.worker.md); ARCHITECTURE.md §8 observability; migration 0005 header (telemetry named as a job of the reconciliation views).
- **Dependencies:** M1-T2 (done).
- **Invariants:** view is `security_invoker = true` and reads as `sk_app` return **only** the session household's rows (INV-TENANT-1 pattern from `tenancy.test.ts`); no household context ⇒ zero rows, never all rows; the view is read-only for `sk_app` (SELECT grant only); metric definition **PROPOSED** as above — the worker implements it verbatim and does not redefine it; the architect records it in ARCHITECTURE §8 at acceptance.
- **Acceptance criteria:** on a seeded ledger with known composition (e.g. 6 statements: 1 `INITIAL_STOCK`, 2 `CONSUME`, 1 user `ADJUSTMENT`, 1 over-consuming `CONSUME` that produced 1 clamp) the view returns `statement_count=5`, `user_adjustment_count=1`, `clamp_count=1`, `correction_event_count=2`, `correction_rate=0.4` exactly; clamp rows are excluded from `statement_count` and counted once; a `system` `ADJUSTMENT` that is not a clamp counts as a statement, not a correction; `since`/`until` boundaries behave inclusive/exclusive; migrate down drops the view cleanly; `pnpm test` with `DATABASE_URL` green, without it the new suite skips loudly with the CI guard (harness pattern).
- **Tests required:** DB tests (DATABASE_URL-gated via `test-support/harness.ts`): composition test above; tenancy test as `sk_app` (household A sees only A; B only B; `null` context sees none); grants test (`sk_app` cannot `INSERT`/`UPDATE` the view); period-bound tests; migration reversibility.
- **File scope:** `apps/api/db/migrations/0007_correction_telemetry.sql`, `apps/api/src/db/telemetry/**` (new), `apps/api/src/db/migrations.test.ts` (if it enumerates migrations), `docs/handoff/M1-T7.worker.md`.
- **Out of scope:** dashboards, alerting jobs (M9), HTTP endpoints (M2), any change to ledger tables or the trigger.
- **DoD:** rule 26; worker report proposes the ARCHITECTURE §8 metric-definition paragraph.

#### M1-T8 — Lot-selection policy for consumption (FEFO/FIFO planner) ✅ DONE 2026-09-15
*Opus worker → independent adversarial Opus review (**PASS WITH FIXES**: duplicate-lot double-allocation, bypassable hand-built-plan overdraw guard, triple-read request — all hand-built-input only, all fixed in scope and mutation-pinned → re-review **PASS**; 17-mutation battery, 15 killed, 2 proven equivalent; reviewer's own 2 000-run zero-clamp property) → squash-merged `244e97b`. 562 tests without DB (+35). FEFO recorded as PROPOSED default in domain-model OQ-1. Handoff: [worker](docs/handoff/M1-T8.worker.md) · [review](docs/handoff/M1-T8.review.md).*
- **Implementation model:** **Opus** — inventory arithmetic that decides how a decrease is split across lots (rule-23 list); a wrong plan produces clamps or drains the wrong stock.
- **Review model:** **Opus** — must adversarially probe ordering ties, expired/undated lots, exhaustion and idempotency-key derivation against the reserved `::` namespace.
- **Objective:** pure domain helper in `packages/domain/src/inventory/lot-selection.ts`: `planLotConsumption(item, { qtyMicros | amount, policy })` → `Outcome<ConsumptionPlan>` where `policy ∈ {"FEFO","FIFO"}`; `ConsumptionPlan = { allocations: [{ lotId, qtyDeltaMicros (<0), lotBalanceBeforeMicros, expiresAt?, expiryTier? }], requestedMicros, allocatedMicros, shortfallMicros }`. FEFO orders lots by `expiresAt` ascending (undated last), ties by `acquiredAt` ascending (undated last), then by lot order on the item; FIFO by `acquiredAt` ascending (undated last), then lot order. Lots at zero balance are skipped. Each allocation ≤ that lot's derived balance, so **a plan never causes an `OVER_CONSUMPTION` clamp**; a request exceeding total on hand returns allocations covering the whole balance plus a non-zero `shortfallMicros` (the caller decides — the planner never invents stock or a negative). Second helper `consumptionInputsFromPlan(plan, base)` produces one `TransactionInput` per allocation from a base input (type/actor/provenance/times/reason/correlation copied by explicit field list), deriving idempotency keys deterministically from `base.idempotencyKey` **without** using the reserved `::` separator (e.g. `<key>/lot/<n>`), so a replay of the whole planned consumption is idempotent and a partial replay conflicts loudly.
- **Context:** [M1-T1 worker §9 item 1](docs/handoff/M1-T1.worker.md); domain-model.md OQ-1 note; brief §7 (meal-log decrements) — the M8 consumer.
- **Dependencies:** M1-T1 (done).
- **Invariants:** INV-LEDGER-4 preserved by construction (applying a plan through `appendTransactions` yields zero clamps — a property test); Σ allocations = min(requested, on hand); determinism (same aggregate + request ⇒ identical plan); no mutation of the input aggregate (deep-frozen inputs untouched); no clock — `asOf`, if offered at all, is a caller-supplied `Instant`.
- **Acceptance criteria:** worked example: lots A (exp 2026-09-20, 1.0 lb), B (exp 2026-09-15, 0.5 lb), C (undated, 2.0 lb); FEFO for 1.2 lb ⇒ B 0.5, A 0.7, C untouched; FIFO with A acquired first ⇒ A 1.0, B 0.2; request 4.0 lb ⇒ all three drained, `shortfallMicros = 500000`; applying plans via `appendTransactions` reconciles with `clampAdjustment` absent on every result; keys never contain `::` and replaying the derived inputs returns `duplicate` for each.
- **Tests required:** example tests above; property tests (fast-check): allocation bounds, Σ rule, ordering monotonicity per policy, zero-clamp-on-apply, determinism, key uniqueness/replay; ties and undated lots explicitly.
- **File scope:** `packages/domain/src/inventory/lot-selection.ts` (+ `.test.ts`), `packages/domain/src/inventory/index.ts` (exports only), `docs/handoff/M1-T8.worker.md`.
- **Out of scope:** which policy the UX defaults to (OQ — architect records **FEFO as PROPOSED default** in domain-model.md at acceptance; PO may overrule); expiry estimation; persistence/API wiring (M2/M8); changing `appendTransaction`.
- **DoD:** rule 26; worker report proposes the domain-model.md OQ-1 wording.

#### M1-T9 — Ledger write retry helper (`40001` + `23505` on the sequence key) ✅ DONE 2026-09-15
*Sonnet worker → independent Opus review on a real Postgres 17 cluster (**PASS WITH FIXES**: the `assumeRole` pass-through — what makes RLS apply to retried writes — had zero test coverage; pinned by a both-attempts role/household/txid test → re-review **PASS**). No double-apply under `COMMIT`-time `40001`; 8-way unlocked contention retried to contiguous sequences; no pool leak. Mutation table reproduced exactly. **Review also surfaced a pre-existing silent-data-loss defect in the repository's `23505` catch → ticketed as M1-T11 (not dispatched).** Squash-merged `12641b4`. 697 tests with DB / 594 without. Handoff: [worker](docs/handoff/M1-T9.worker.md) · [review](docs/handoff/M1-T9.review.md).*
- **Implementation model:** Sonnet — a bounded retry wrapper with a precisely specified error classifier; the hard part (which errors mean "retry") is already pinned by the constraints suite and stated below.
- **Review model:** **Opus** — concurrency-sensitive inventory updates (rule-23 list); the reviewer must verify that a business conflict is never retried and that a retry cannot double-apply.
- **Objective:** `apps/api/src/db/retry.ts`: `isRetryableLedgerError(error)` — true for SQLSTATE `40001` (any), and for `23505` **only when** `constraint === "inventory_transactions_item_sequence_key"`; false for every other `23505` (notably `inventory_transactions_idempotency_key`, a terminal business conflict already mapped by the repository) and every other error. `withRetriedHouseholdTransaction(pool, householdId, fn, options)` wraps `withHouseholdTransaction`, re-running the **whole** transaction on a retryable error up to `maxAttempts` (default 3) with an injectable `delay(attempt)` and injectable `sleep` (tests never wait on real time and never use `Math.random`); on exhaustion throws `LedgerRetryExhaustedError` carrying `attempts` and `cause`; non-retryable errors propagate unchanged after attempt 1.
- **Context:** [M1-T2 worker §11 item 4 and §12 F6](docs/handoff/M1-T2.worker.md) (both codes are the same concurrency loser); repository header comment (the `40001` trigger backstop); migration 0004.
- **Dependencies:** M1-T2 (done).
- **Invariants:** retries re-execute `fn` from scratch inside a fresh transaction — no partial state survives (rollback proven); idempotency conflicts are **never** retried; a retried append is idempotent by the ledger's own key semantics, so a retry can never double-count (test: the successful retry leaves exactly one row for the key).
- **Acceptance criteria:** classifier unit tests over a table of `{code, constraint}` pairs (pure, no DB); DB tests (gated): (a) two concurrent appends to one item through the locked repository path — the loser hits `40001` or blocks-and-succeeds, and with the helper both succeed with the ledger showing both rows and contiguous sequences; (b) a forced stale-sequence insert path that raises `23505` on the sequence key is retried and succeeds; (c) an idempotency-key conflict is **not** retried (`attempts === 1`) and surfaces as the repository's typed `IDEMPOTENCY_KEY_CONFLICT`; (d) a permanently failing `fn` exhausts after `maxAttempts` with the original cause attached; (e) a non-retryable error propagates after one attempt.
- **Tests required:** as above; the fake `sleep` records the delays requested.
- **File scope:** `apps/api/src/db/retry.ts` (+ `.test.ts`), `apps/api/src/db/session.ts` (only if a small hook is needed — document why), `docs/handoff/M1-T9.worker.md`.
- **Out of scope:** HTTP-layer retry semantics, backoff tuning under load (M9), changing the trigger or constraints.
- **DoD:** rule 26.

#### M1-T10 — Maintenance bundle (accepted follow-ups, no behaviour change to product logic) ✅ DONE 2026-09-15 — **D-018 BATCH COMPLETE**
*Sonnet worker → independent Sonnet review (**PASS**, no fixes: every item re-verified first-hand — registry digest, GS1 digit, OFF non-collision, lint failure, four mutations killed) → squash-merged `605aae2`. All 13 items landed, none reverted. 769 tests with DB / 665 without (+71). Handoff: [worker](docs/handoff/M1-T10.worker.md) · [review](docs/handoff/M1-T10.review.md).*
- **Implementation model:** Sonnet — tooling, pins, fixtures and test-coverage items; each is small and mechanical.
- **Review model:** Sonnet — review checks each item against its acceptance line and that no product behaviour changed; item (e) touches the ledger write path and gets a line-by-line look.
- **Objective:** land these accepted follow-ups in one branch, one commit per item, in this order (items (a)–(m); (l) added at M1-T7 acceptance, (m) at M1-T9 acceptance):
  - **(a)** `packageManager` pnpm `12.3.1 → 12.3.4` and add `workflow_dispatch:` to `ci.yml` (M0-T2). Must install with `--frozen-lockfile` locally and in CI; if the local corepack cannot fetch 12.3.4, **revert (a) and report** rather than work around.
  - **(b)** pin the CI `postgres:17-alpine` image by digest (`image: postgres:17-alpine@sha256:…`), digest obtained read-only from the registry (no Docker locally); comment how to bump.
  - **(c)** tighten the domain boundary lint: `import-x/no-extraneous-dependencies` for `packages/domain/src/**` production files must reject devDependencies (`devDependencies: false`), so a `fast-check` import into production domain source fails lint; prove it with a temporary violation (shown in the report, then removed) and correct the overclaiming comment.
  - **(d)** `scripts/**/*.mjs` lint block: modern `ecmaVersion`/`sourceType: module` + Node globals (M1-T5 follow-up); `pnpm lint` stays green.
  - **(e)** canonicalise lot instants on write: `insertInventoryLot` passes `acquiredAt`/`expiresAt` through `canonicalizeInstant`; the round-trip property generator produces non-canonical offsets/precisions and the read-back equals the canonical form (M1-T2 F9).
  - **(f)** move pure helpers (`microsToDecimalText`, `canonicalizeInstant`, mapping validators) under unit tests that run **without** `DATABASE_URL` (M1-T2 F10) — no behaviour change.
  - **(g)** `next_sequence`-drift read-path test: tamper `inventory_items.next_sequence` inside a rolled-back transaction (existing F5 pattern) and assert `loadInventoryItem` refuses with `CORRUPT_LEDGER`.
  - **(h)** UUIDv7 helper `apps/api/src/ids/uuidv7.ts` (RFC 9562 layout from `node:crypto`; version/variant bits, millisecond timestamp, monotonic within a millisecond) with tests — **not wired anywhere**: where ids are minted stays an M2 decision.
  - **(i)** corpus-mirror consistency test: outside `packages/domain` (which stays I/O-free), assert the inline recommendations-corpus mirror equals the JSON fixtures on disk, so the two cannot drift silently (M1-T4 follow-up). Include the M1-T6 fixture (`screening-016`). Also correct `tests/fixtures/recommendations/README.md`, which cites a non-existent `packages/domain/src/allergens/fixtures.test.ts` — point it at the real mirror (`corpus.test.ts`) and at the new consistency test (M1-T6 worker item 3).
  - **(j)** one sesame-bearing product fixture in `tests/fixtures/products/**` following the corpus's synthetic-code scheme (valid GS1 check digit; non-collision spot-checked read-only against Open Food Facts as M1-T5 did, or reported as unchecked if offline), and update the domain corpus test's "sesame absent" note/assertion so all nine codes are exercised.
  - **(k)** CONTRIBUTING.md note on running the database suites locally (`DATABASE_URL`, loud skip, Docker or any throwaway Postgres incl. the native `initdb` pattern).
  - **(m)** *(added at M1-T9 acceptance)* retry-helper nits in `apps/api/src/db/retry.ts` + its tests only: wrap the `sleepError.cause = ledgerError` assignment in `try/catch` so a frozen/non-extensible rejection cannot throw and lose both errors (review F8); add one fake-pool test that a rejecting injected `sleep` propagates with the ledger error as `cause` — mutation "delete `throw sleepError`" must be killed (review F9); validate `delay()`'s return (finite, ≥ 0) or document that it is caller-owned (review F3). No change to classifier semantics.
  - **(l)** *(added at M1-T7 acceptance)* telemetry test hardening: mismatched-id tenancy test (session context A, `readCorrectionTelemetry(c, B.id)` ⇒ empty) + Beta `household_id` identity assertion in `view.test.ts` (review F1/F3); and a view-vs-read-function equivalence test over a generated multi-item composition so the duplicated aggregate column list cannot drift (review B2). `apps/api/src/db/telemetry/**` tests only — no SQL change.
- **Context:** "Accepted follow-ups" section below (M0-T2, M1-T2, M1-T3, M1-T4, M1-T5 lines).
- **Dependencies:** M1-T6 (fixture for (i)), M1-T9 (avoid touching the same files concurrently — this ticket runs last).
- **Invariants:** no product-logic behaviour change; `packages/domain` still zero deps / zero I/O; all existing 475 tests still pass (with DB) — nothing weakened or deleted; no new runtime dependency (rule 11 — `uuidv7` is hand-written on `node:crypto`, no package).
- **Acceptance criteria:** one commit per item, each item's line above satisfied; `pnpm install --frozen-lockfile && pnpm lint && pnpm typecheck && pnpm test` green locally (DB suites skip loudly) and CI green with the DB suites running; report has a table item → commit → evidence.
- **Tests required:** per item as stated; (f) and (h) add non-DB unit tests; (e), (g), (i), (j) add or extend tests.
- **File scope:** `package.json`, `pnpm-lock.yaml`, `.github/workflows/ci.yml`, `eslint.config.js`, `apps/api/src/db/**` (items e–g only), `apps/api/src/ids/**`, `packages/adapters/src/**` or `tests/**` (item i), `tests/fixtures/products/**` (+ manifest/README lines), `packages/domain/src/allergens/corpus.test.ts` (item j note only), `CONTRIBUTING.md`, `docs/handoff/M1-T10.worker.md`.
- **Out of scope:** SHA-pinning the GitHub actions themselves (M9); the `SECURITY DEFINER` owner role (needs the hosting decision); `fl-oz` guard (conditional, not needed); anything in `docs/**` other than the handoff report.
- **DoD:** rule 26; worker report's item table complete; any item reverted is listed with the reason.

#### M1-T11 — Ledger write path: aborted-transaction COMMIT silently discards work ✅ DONE 2026-09-16
*Approved under D-018a. Opus worker → independent Opus review on a real Postgres 17 cluster (**PASS WITH FIXES**: the fn-thrown-`TransactionAbortedAtCommitError` rollback path had zero coverage and its mutant leaked an open `sk_app` transaction with the previous household context to the next pool borrower; pinned by a same-connection test → re-review **PASS**). F4 repro: pre-fix 0 durable rows reported as success; post-fix 1 durable row. Ten further loss-path scenarios clean. Squash-merged `13a631b`. 786 tests with DB / 668 without. data-model §6 gained the savepoint-scoped rejection + "COMMIT is not proof of commit" rules. Handoff: [worker](docs/handoff/M1-T11.worker.md) · [review](docs/handoff/M1-T11.review.md).*
*Found by the M1-T9 reviewer (F4) while hunting double-applies; independently reproduced on Postgres 17.10. Not introduced by any M1-E3 ticket. Not reachable in production today (the repository has no production callers yet), but it is silent data loss in the ledger write path and must land before M2 wires HTTP handlers.*
- **Implementation model:** **Opus** — ledger write-path transaction semantics (rule-23 list: inventory ledger, idempotency).
- **Review model:** **Opus** — must prove no committed-but-reported-rejected or rejected-but-reported-appended outcome remains; adversarial on savepoint/abort states.
- **Objective:** `appendTransactionToDb` catches the idempotency-key `23505` **without a `SAVEPOINT`** and returns `ok({ status: "rejected" })`. Postgres is then in ABORTED state; `withHouseholdTransaction` issues `COMMIT`, which Postgres answers with the `ROLLBACK` command tag **and no error**, so the caller receives success while every earlier write in that transaction is discarded. Repro: one `fn`, a clean append, then a second append reusing a burnt key ⇒ returns `{ good: "appended", bad: "rejected" }` and the item's ledger has 0 rows. Fix both layers: (1) `repository.ts` — wrap the insert block in `SAVEPOINT`/`ROLLBACK TO SAVEPOINT` so the typed rejection leaves the transaction usable; (2) `session.ts` — inspect the `COMMIT` result's command tag and **throw** if it is `ROLLBACK` (fail loud, never report success for a rolled-back transaction).
- **Context:** [M1-T9 review F4/P9](docs/handoff/M1-T9.review.md); ADR-008 durability; data-model.md §6 idempotency; INV-LEDGER-3.
- **Dependencies:** M1-T9 (done). Should precede M2's first HTTP write handler.
- **Invariants:** a transaction reported `appended` is durable after `withHouseholdTransaction` returns; a typed `rejected` never rolls back sibling writes; no double-apply introduced; RLS/role behaviour unchanged.
- **Acceptance criteria:** the repro above returns `appended` + `rejected` AND the good row is durable (1 row); a deliberately aborted transaction (`SELECT 1/0` inside `fn`, error swallowed) makes `withHouseholdTransaction` throw rather than return; existing `23505` idempotency mapping and the M1-T9 retry helper behave unchanged (retry suites green); reviewer reproduces both before/after on a real cluster.
- **Tests required:** DB-gated tests for both repros (reviewer runs the reverted versions to prove they fail before the fix); a savepoint-release test (no lingering savepoint after success); full gate with `DATABASE_URL`.
- **File scope:** `apps/api/src/db/inventory/repository.ts`, `apps/api/src/db/session.ts`, their tests (`apps/api/src/db/inventory/*.test.ts`, new `apps/api/src/db/session.test.ts`), `docs/handoff/M1-T11.worker.md`.
- **Out of scope:** HTTP layer; retry-helper changes; schema changes.
- **DoD:** rule 26; data-model.md §6 note on savepoint-scoped idempotency rejection (architect applies).

---

## Milestones 2–9 (epic level only — ticketed when reached)

### M2 — Household + inventory API
Auth integration (managed provider — ADR-004 vendor decision here; cost approval gate), household CRUD + membership, inventory endpoints over the ledger (all writes idempotent), authz matrix + INV-TENANT-1 suite, structured logging/correlation IDs, first deploy target decision (PaaS — cost approval gate). *Exit:* two fixture users in one household mutate shared inventory via HTTP with full isolation tests green. **2026-09-21 (D-022): M2 starts against a stubbed identity port; the vendor decision is not consumed.**

#### M2-T1 — Identity port with fixture implementation, request context, default-deny authorization, first read endpoint ✅ DONE 2026-09-22
*Opus worker (isolated worktree) → independent adversarial Opus review (**PASS WITH FIXES**: the compiled API could not start because the workspace packages exported TypeScript source; every 500 logged `[object Object]`; the production guard was an exact-string denylist; four minor) → fixes → **architect finding F8** (from an empty build state `pnpm lint` failed on the new `dist` exports; CI lints before typecheck) → fix with a `source` export condition → re-review **PASS** → squash-merged. Tenancy core survived every attack incl. five mutation checks. **Scope extension ratified by the architect for F1/F8:** `packages/{contracts,domain,adapters}/package.json` (exports `source`/`types`/`default`), root `vitest.config.ts` (source aliases), `eslint.config.js` (resolver `conditionNames`); `app.test.ts` rewrite, `server.test.ts`, `apps/api/tsconfig.json`, lockfile accepted. 1113 tests with DB / 961 without / 956 with no build. Handoff: [worker](docs/handoff/M2-T1.worker.md) · [review](docs/handoff/M2-T1.review.md). Follow-ups: see "Accepted follow-ups".*
- **Implementation model:** **Opus-class** — household authorization and tenancy isolation (rule 23 list). *PO 2026-09-22: default Opus is fine; the architect reviews the code in detail at acceptance, on top of the independent review.*
- **Review model:** **Opus-class** — same reason; the reviewer must try to reach another household's rows through every layer (HTTP, service, SQL role).
- **Objective:** in `apps/api`: (a) an `IdentityPort` (`resolveSession(token) → { userId, householdId, role } | null`) with a **fixture implementation** exposing two households (Chen: owner Dean `DC`, member Maya `MC`; and a second household with one owner) selected by a bearer token from `tests/fixtures/identity/`; the fixture is registered only when `SK_IDENTITY=fixture` and the process is not `NODE_ENV=production` (refuses to start otherwise); (b) a Fastify plugin that resolves every request to `(user, household, role)` via the port, attaches a correlation id, and **denies by default** (no session → 401; a route without an explicit authorization declaration → 403 in tests via a registration-time check); (c) the request-scoped DB session sets the tenant context the schema's RLS expects (M1-T2 `session.ts` pattern, `assumeRole` pinned), and never runs SQL outside it; (d) one endpoint, `GET /v1/inventory/items`, returning the household's inventory snapshot rows with per-field tier (`packages/contracts` types added); (e) structured JSON logging with the correlation id and the redaction denylist from ARCHITECTURE §7.15.
- **Context:** [ARCHITECTURE.md §3, §7, §8](ARCHITECTURE.md); [data-model.md §5](docs/architecture/data-model.md); [ADR-004](docs/adr/ADR-004-authentication.md) + D-022; M1-T2 schema and `apps/api/src/db/session.ts`; M1-T11 (COMMIT tag rule); INV-TENANT-1 in [testing-strategy.md](docs/architecture/testing-strategy.md).
- **Dependencies:** M1-T2, M1-T9, M1-T11 (all done). No M3 dependency.
- **Invariants:** INV-TENANT-1 (no cross-household read or write through any path); identity comes only from the port (no header, query or body may name a household); the fixture adapter cannot load in production; every DB statement runs inside the tenant-scoped session; no secrets (fixture tokens are obviously fake strings, documented as such).
- **Acceptance criteria:** with `SK_IDENTITY=fixture`, Dean's token lists Chen inventory; Maya's token lists the same rows; the second household's token lists only its own rows; no token → 401; a made-up token → 401; a request carrying a forged `x-household-id` header is ignored; starting with `NODE_ENV=production SK_IDENTITY=fixture` exits non-zero with a clear message; every log line carries the correlation id and none carries an email, name or token; `pnpm lint typecheck test format:check` green; CI green.
- **Tests required:** HTTP-level tenancy suite over a throwaway DB (the M1-T2 test-support pattern): the three tokens × the endpoint matrix; 401/403 paths; header-forgery test; production-refusal test; a **mutation-style** test that removes the tenant context and proves the endpoint then returns nothing (RLS fail-closed, not app-layer luck); log redaction test.
- **File scope:** `apps/api/src/identity/**` (new), `apps/api/src/http/**` (new: plugin, routes, logging), `apps/api/src/app.ts`, `apps/api/src/server.ts`, `packages/contracts/src/**` (inventory read DTOs), `tests/fixtures/identity/**` (new), `apps/api/package.json` (only if a dependency is justified per rule 11; Fastify plugins preferred over new packages), `.env.example` (names only), `docs/handoff/M2-T1.worker.md`.
- **Out of scope:** any real auth vendor; write endpoints (M2-T2); household CRUD (M2-T3); deployment; the client.
- **DoD:** rule 26; worker report proposes the ARCHITECTURE §7 / data-model §5 wording changes, applied by the architect at acceptance.

### Epic M3-E0 — UX design (GATES the M3 build; PO directive 2026-09-10: plan only, no build)
Deliverables, per [docs/design/ux-plan.md](docs/design/ux-plan.md) §4: wireframes for the 12 MVP screens incl. non-happy states → hi-fi mockups in the design-direction tokens (extending the existing clickable prototype) → safety-copy deck (M1-T4's nine rules applied verbatim, reviewed like code) → PO + Dean on-device review + one real-household hallway test of the three make-or-break flows → iterate once. **Exit gate: PO sign-off recorded in DECISIONS *and* D-002 ratified — only then are M3 build tickets written.** Design work is Sonnet-class with PO review; no code, no dependencies. Open inputs: Q8 brand, OQ-D4/D5/D6/D7/D8 ([ux-plan §5](docs/design/ux-plan.md)). **Progress 2026-09-15 (D-019):** the PO's v3 prototype + competitor-research plan are adopted as the visual direction; hi-fi coverage for S3/S4/S6/S7/S8/S10/S11 exists; the allergen-copy violations were fixed at adoption. Remaining: S1/S2/S5/S9/S12 + non-happy states + provenance legend/undo/who-did-what ([ux-plan §7](docs/design/ux-plan.md)), terracotta scarcity pass, contrast measurements, the safety-copy deck, Dean's on-device review, hallway test.

#### M3-E0 tickets (written 2026-09-15; approved D-018a; **all three DONE 2026-09-16**). Remaining M3-E0 steps are PO-run: Dean's on-device review of the completed prototype, one household hallway test of the three flows, the PROPOSED token darkenings call, then sign-off in DECISIONS **and** D-002 ⇒ M3 build tickets.

#### M3-E0-T1 — Prototype completion: missing MVP screens and non-happy states (design, docs only) ✅ DONE 2026-09-16
*Sonnet worker → independent Opus review (**PASS WITH FIXES**: skippable allergy gate; S5 ledger not reconciling with an inverted clamp sign; correct-in-place instead of an appended Adjustment; caveat/evidence missing on BLOCKED states; a dev control inside the frame; six unaudited terracotta uses; missing tier chips → all fixed) → re-review **PASS** (one caveat line applied by the architect) → squash-merged `05181fb`. 14 screens; three make-or-break flows complete with zero dead taps. Handoff: [worker](docs/handoff/M3-E0-T1.worker.md) · [review](docs/handoff/M3-E0-T1.review.md).*
- **Implementation model:** Sonnet — HTML/CSS prototype work inside an established token system; no product logic.
- **Review model:** **Opus** — allergen and provenance surfaces are safety copy (P5, SR-1/2); the reviewer greps the prototype against the nine copy rules, P11, and the three verdict states.
- **Objective:** extend `docs/design/mockups/smart-kitchen-prototype.html` (same file, tokens and back-stack nav) with the screens ux-plan §7 marks missing: **S1** account + household (create/join, members, sign-out); **S2** allergies & preferences (per-member allergy + severity, user-defined allergen, explicit "none", not skippable); **S5** item detail/history (ledger view "why does the app think…", one-tap correct with undo, consume/discard with reason chips); **S9** barcode-miss → manual completion (code retained for enrichment); **S12** profile & household; **recipe detail** (per-ingredient on-hand status, verdict block with evidence and member attribution, "Cook this" → USE_IN_MEAL confirm listing the FEFO deductions); non-happy states: first-run empty inventory, camera permission denied, offline shopping list with queued check-offs, zero candidates / all blocked; shared: one-time provenance legend, undo toast on every correction and check-off, who-did-what initials, inventory sort/filter (expiry, needs confirmation).
- **Context:** [ux-plan.md §2, §3, §7](docs/design/ux-plan.md); [design-direction.md §0, §7](docs/design/design-direction.md); [design-principles.md](docs/design/design-principles.md) P1–P11; D-019; M1-T8 planner semantics; M1-T4/T6 verdict payloads.
- **Dependencies:** D-019 (done). OQ-D5 default (owner-enters-all) unless the PO decides otherwise.
- **Invariants:** no "safe"/"passed"/green-check clearance anywhere; ALLOWED renders "no known allergen match" + caveat; BLOCKED never offered as a choice; every fallible value wears its tier; **zero em dashes** (P11); terracotta only on primary CTA, active nav, hero accent (scarcity pass included); every screen designs empty/loading/error/offline where applicable (P8); 44pt targets, colour never the sole signal (P9).
- **Acceptance criteria:** all 12 S-screens navigable from the five-tab shell at 390×844 and on the PO's Galaxy device; `grep -c "—"` on the file = 0; visible copy contains no "safe"/"passed" (except the assistant's "can't mark any food as safe"); all three verdict states appear on at least one card; the three make-or-break flows (ux-plan §3) complete with zero dead taps.
- **Tests required:** reviewer copy audit (grep table) + PO on-device check.
- **File scope:** `docs/design/mockups/smart-kitchen-prototype.html`, `docs/design/ux-plan.md` §7 status column only, `docs/handoff/M3-E0-T1.worker.md`.
- **Out of scope:** any client code; receipt/photo flows beyond stubs; assistant beyond the preview; dark mode (OQ-D6).
- **DoD:** rule 26; ux-plan §7 all rows "done".

#### M3-E0-T2 — Safety and provenance copy deck (docs only) ✅ DONE 2026-09-16
*Sonnet worker (isolated worktree) → independent Opus review (**PASS WITH FIXES**: caveat waived on cards, a warning string false off `ALLOWED`, a wrong description of the engine, no severity on compact surfaces, non-canonical caveat → all fixed, plus nine smaller items incl. a P9 accessibility-label rule) → re-review **PASS** → squash-merged `b4e4c4f`. 63/63 machine codes covered; zero em dashes; the deck is now the binding string source for M3 allergen/provenance surfaces. Handoff: [worker](docs/handoff/M3-E0-T2.worker.md) · [review](docs/handoff/M3-E0-T2.review.md) · [deck](docs/design/copy-deck.md).*
- **Implementation model:** Sonnet — writing to a fixed rule set.
- **Review model:** **Opus** — this deck becomes the client's string table for allergen surfaces; reviewed like code against the nine rules, D-017, P11.
- **Objective:** `docs/design/copy-deck.md`: every user-visible string for allergen verdicts (per `ScreeningVerdict` × `UnknownReason` × `WarningCode` from `packages/domain/src/allergens/types.ts`), provenance chips and legend, correction language (teaching, not apologising, P3), shortfall/clamp prompts, confirmation trays, phase labels, empty/offline/error states, the standing no-guarantee caveat. Each string keyed by machine code (never by message text), with tone notes and forbidden alternatives.
- **Context:** [M1-T4 worker §6](docs/handoff/M1-T4.worker.md) nine rules; [M1-T6 review](docs/handoff/M1-T6.review.md) F7 (`ALLOWED` can co-occur with `UNRECOGNIZED_ALLERGEN_DATA`); D-017; P3/P5/P11.
- **Dependencies:** none (parallel with T1).
- **Invariants:** the nine rules verbatim; zero em dashes; no exclamation marks in safety copy; every union member has a string.
- **Acceptance criteria:** code → string coverage table complete against the type unions (reviewer diffs against `types.ts`); forbidden-words grep (`safe`, `passed`, `allergen-free`, `guaranteed`, `—`) returns zero hits outside the "forbidden" column.
- **File scope:** `docs/design/copy-deck.md`, `docs/handoff/M3-E0-T2.worker.md`.
- **Out of scope:** i18n; marketing copy; assistant dialogue.
- **DoD:** rule 26; the M3 epic links the deck as binding.

#### M3-E0-T3 — Token sheet finalisation: contrast measurements and scarcity audit (docs only) ✅ DONE 2026-09-16 — **D-018a QUEUE COMPLETE; engineering stopped for the PO/Dean session**
*Sonnet worker (relaunched after a stalled first attempt) → independent Sonnet review (**PASS WITH FIXES**: five uncovered text-on-card contrast pairs incl. the Estimated badge and expiry text; a 1px hit-slop slip; a count derivation → fixed) → re-review **PASS**, all 40 ratios independently recomputed with zero discrepancy → squash-merged `804881e`. Output: [docs/design/tokens.md](docs/design/tokens.md) — zero token drift; 16 contrast FAILs each with a PROPOSED fix (7 distinct darkenings, **PO visual call**); 12 of 15 tap targets under 44px with remediation; reduced-motion treatment per declaration. Handoff: [worker](docs/handoff/M3-E0-T3.worker.md) · [review](docs/handoff/M3-E0-T3.review.md).*
- **Implementation model:** Sonnet — measurement and tabulation.
- **Review model:** Sonnet — checks the arithmetic and that no token drifted from the prototype.
- **Objective:** turn design-direction §0 into `docs/design/tokens.md`: a measured WCAG contrast ratio for every text/background pair used in the prototype (incl. `#c9b9a4` on espresso, cream on the darkest scrim stop, amber on `amber-bg`, ink-3 captions on paper); flag pairs under 4.5:1 body / 3:1 large / 7:1 alert text with a proposed fix; audit brand-accent usage per screen against the scarcity rule (every terracotta occurrence marked keep/remove). Ready to become the typed token module in M3.
- **Context:** design-direction §0, §3 rule 4, §7 "still to fix"; P9.
- **Dependencies:** T1 (audit the completed prototype).
- **Acceptance criteria:** every pair measured with the formula shown; failures have fixes; scarcity table complete; no token differs from the prototype's `:root` block.
- **File scope:** `docs/design/tokens.md`, `docs/design/design-direction.md` §0 (value corrections only), `docs/handoff/M3-E0-T3.worker.md`.
- **DoD:** rule 26.

*(M3-E0 step 4 — Dean's on-device review + one household hallway test — is PO-run, not a ticket. Exit gate unchanged: PO sign-off in DECISIONS **and** D-002 ratified ⇒ M3 build tickets.)*

### M3 — Basic client inventory experience
**Build opened 2026-09-21 (D-023) for the locked screens; source of truth for every screen is prototype v4 (`docs/design/mockups/smart-kitchen-prototype.html`) and, for every allergen/provenance string, [copy-deck.md](docs/design/copy-deck.md). Held until Dean's list is triaged: Menu page internals (D-020) and Home dashboard states (OQ-D9).** Build order: M3-T1 scaffold + tokens + shell → M3-T2 onboarding (S1, S2) → M3-T3 inventory list + item detail (S4, S5) → M3-T4 add food (S6 to S9) → M3-T5 shopping (S11) → M3-T6 profile (S12); Menu and Home tickets written when D-020 is ratified. Client talks to the API of M2-T1 onward through fixture identity (D-022); until an endpoint exists, screens read from `packages/adapters` fixtures behind the same client port.

Expo app (ADR-001 finalize): auth, household join, inventory list by location, manual add/adjust/deplete with reasons, item history view ("why does the app think…"), barcode scan → lookup → confirm flow with provenance badges (Known Fact / Estimated / AI Interpretation), correction-rate telemetry live.

#### M3-T1 — Expo app scaffold, design tokens module, tab shell with Menu placeholder, fixture identity client ✅ DONE 2026-09-22
*Sonnet worker (isolated worktree) → independent Sonnet review (**PASS WITH FIXES**: the mobile domain-boundary lint could be bypassed by a relative path into `packages/domain/src` → `import-x/no-restricted-paths` zone + 3 tests) → re-review **PASS** → squash-merged `11e79fb`. Expo SDK 57 (react 19.2.3, react-native 0.86.3 pinned to what the SDK bundles), Fraunces + Inter vendored under OFL, tokens pinned to D-021 with a live WCAG recomputation, five-slot shell with the Menu placeholder, fixture ApiClient with no network, CI export smoke check. 731 tests without a DB. Handoff: [worker](docs/handoff/M3-T1.worker.md) · [review](docs/handoff/M3-T1.review.md). Accepted follow-ups: see "Accepted follow-ups" below.*
- **Implementation model:** Sonnet — tooling, config and shell UI inside an established token system; no product logic.
- **Review model:** Sonnet — no rule-23 risk; the reviewer checks the token numbers against D-021, the dependency justifications (rule 11), CI integration, and that no screen content beyond the shell was built.
- **Objective:** create `apps/mobile` as an Expo (managed workflow, TypeScript, Expo Router) app inside the pnpm workspace so that `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm format:check` cover it and CI stays green. Deliver: (a) `apps/mobile/src/design/tokens.ts`, the single source of colour, type scale, radius and spacing values, **exactly the D-021 palette** (`--ink-3` `#665c52`, `--amber` `#925b0a`, `--green` `#2c744b`, `--rose` `#9d4b51`, `--danger` `#882020`, `--brand` `#d9673b`, `--brand-deep` `#b64f28`, pressed `#9c4322`, `--brand-tint` `#f9dece`, `--brand-on-tint` `#a74925`, sand/paper/line/ink/espresso/cream/ai values from design-direction §0) with a unit test that pins every hex; (b) fonts: Fraunces (display) and Inter (body) loaded via `expo-font` from files vendored under `apps/mobile/assets/fonts/` with their OFL licence files; (c) the shell: five-slot bottom bar exactly as prototype v4 (Home, Inventory, centre scan button, **Menu**, Shopping), 44pt targets, active state in `--brand-deep` text with `--brand` icon stroke, each tab a placeholder screen whose only content is the screen name and "coming in M3-Tn"; the centre button opens a placeholder Add screen; (d) an `ApiClient` port with a fixture implementation that supplies the fixture identity token (matching M2-T1's `tests/fixtures/identity/` tokens) and a `getInventoryItems()` that returns adapter fixtures; no network calls yet; (e) `pnpm --filter mobile start` runs Expo Go locally; `pnpm --filter mobile export` (web or a platform bundle) is the CI build smoke check; (f) `apps/mobile/README.md` with run instructions for Windows and for Dean's phone over the LAN, no tunnel, no account required.
- **Context:** [ADR-001](docs/adr/ADR-001-client-platform.md) (DECIDED), D-005, D-021, D-022, D-023; [design-direction.md §0](docs/design/design-direction.md); [tokens.md](docs/design/tokens.md) §1, §2, §5, §6; [design-principles.md](docs/design/design-principles.md) P9, P11; prototype v4 `.nav`, `.fab`, `.phone` CSS; [ARCHITECTURE.md §2](ARCHITECTURE.md) (client thin, `packages/contracts` is the seam); [CONTRIBUTING.md](CONTRIBUTING.md).
- **Dependencies:** M0-T1, M0-T2. Independent of M2-T1 (only the fixture token strings are shared; if M2-T1 has not merged, define them in `tests/fixtures/identity/README.md` and M2-T1 adopts them).
- **Invariants:** no vendor SDK or network call; the domain package is never imported by UI code except through `packages/contracts` types (extend the boundary lint to `apps/mobile`); zero em dashes in any string (P11); no colour outside `tokens.ts`; no secrets; every new dependency justified in the worker report (why, alternatives, licence) and kept to Expo's own modules plus `expo-router`, `expo-font`, `react-native-safe-area-context`, `react-native-screens` unless the worker escalates; Windows long paths respected (CONTRIBUTING note).
- **Acceptance criteria:** `pnpm install --frozen-lockfile` succeeds; root `lint`, `typecheck`, `test`, `format:check` green with the new app included (root `tsconfig.json` references, vitest include, eslint files); the export smoke check passes in CI; the tokens test pins all D-021 values; the tab bar renders five slots in the prototype's order with the Menu label and a calendar-style icon; tapping each tab shows its placeholder; `apps/mobile/README.md` lets Dean run it in Expo Go on his phone against Andy's machine on the same Wi-Fi; `pnpm audit --audit-level=high` clean or documented.
- **Tests required:** `tokens.test.ts` (every hex pinned to D-021, contrast of the seven fixed pairs recomputed >= threshold); a render test of the tab shell (five slots, labels, order) using `@testing-library/react-native` if adopted per rule 11, otherwise a pure test of the route config; fixture `ApiClient` test; boundary lint test proving `apps/mobile` cannot import `@smart-kitchen/domain` internals.
- **File scope:** `apps/mobile/**` (new), `packages/adapters/src/**` (only to export the existing inventory fixtures for the client, no new fixtures), `packages/contracts/src/**` (client-facing DTOs only if M2-T1 has not defined them), root `package.json`, `pnpm-workspace.yaml`, `pnpm-lock.yaml`, `tsconfig.json`, `vitest.config.ts`, `eslint.config.js`, `.prettierignore`, `.gitignore`, `.github/workflows/ci.yml` (add the export smoke step only), `apps/README.md`, `CONTRIBUTING.md` (mobile run notes only), `tests/fixtures/identity/README.md`, `docs/handoff/M3-T1.worker.md`.
- **Out of scope:** any real screen content (S1 to S12 are M3-T2 onward); Menu page internals and Home states (held, D-023); real auth; EAS, store accounts, tunnels or any paid or external resource (rule 17); dark mode (OQ-D6); a UI component library (rule 11, decide in M3-T2 with concrete needs).
- **DoD:** rule 26; worker report lists every dependency added with its justification and the exact Expo SDK version; `apps/README.md` and CONTRIBUTING updated in-scope; STATUS updated by the architect at acceptance.

#### M3-T2 — Onboarding: account + household (S1) and allergies + preferences (S2), first-run routing, onboarding state behind the client port ✅ DONE 2026-09-22
*Sonnet worker (isolated worktree; a first worker stalled before writing and was relaunched) → independent Opus review (**PASS WITH FIXES**: copy scan blind to JSX text; deep links to the tab routes bypassed the allergy gate; `@types/node` globals leaked program-wide; Home copy matched neither prototype nor deck; severity rows Title-Cased user wording; gate refusal not announced; Skip preferences under 44pt; seven minors) → fixes → re-review found a stale-read redirect that bounced S2 Continue back to S2 (F18) → fix → **PASS** → squash-merged. Architect rulings: prototype v4 wins on non-safety copy, the deck on allergen/provenance/voice/forbidden words (recorded in the deck header); "Crustacean shellfish" from the taxonomy labels; the port takes an explicit `noneConfirmed`, never inferred from an empty list; code comments exempt from P11; `eslint.config.js` (Node-global containment rules) and `packages/contracts/src/household.test.ts` ratified as scope. 1095 tests without a DB. Deck §11 added. Handoff: [worker](docs/handoff/M3-T2.worker.md) · [review](docs/handoff/M3-T2.review.md).*
- **Implementation model:** Sonnet. Screens inside the established token system and shell; the only logic is a small gating state machine.
- **Review model:** **Opus-class**. The allergy step is safety copy and a safety gate (P5, D-017, SR-1/SR-2): the reviewer greps every string against copy-deck §2 and §10, checks the explicit-"none" gate cannot be skipped, and checks the danger colour is used only where tokens.md §4 allows.
- **Objective:** in `apps/mobile`, build the two onboarding screens from prototype v4 (`#scr-account`, `#scr-allergies`) and wire first-run routing. (a) **S1 Welcome:** "Continue with email" signs in as the fixture identity (`fixture.dean.chen`, D-022) through the `ApiClient` port; "Continue with Google" is shown with a phase label and explains on tap that it arrives with the auth vendor (P10, scope honesty); create a household (name required, trimmed, 1 to 60 characters) or join with a code (the fixture accepts `CHEN-482` only; any other code gets the honest error "That code didn't match a household. Check it with whoever invited you."). (b) **S2 Allergies:** per member of the household (fixture: Dean and Maya; owner enters for everyone, OQ-D5 default), the nine major allergen chips labelled from `MAJOR_ALLERGEN_LABELS` in sentence case, a Standard / Severe choice per selected allergen (default Standard), a free-text "Add another allergen or ingredient" that becomes a `USER_DEFINED` restriction at Standard severity, and the explicit option "No known allergies for {member}" which is mutually exclusive with any selection; **Continue is refused until every member has at least one restriction or the explicit none**, with the prototype's message "Select at least one allergen, or confirm none, to continue" shown inline (not only as a toast); preferences chips (Vegetarian, High protein, Kid-friendly) are optional with a "Skip preferences" link. (c) **Routing:** a new user (no household in the client state) lands on S1, then S2, then Home (the placeholder from M3-T1 for now, with the first-run copy "Your kitchen is empty" and a scan button that opens the Add placeholder); a returning user lands on Home; the back gesture from S2 returns to S1 and never skips the gate. (d) **Client port:** extend `ApiClient` with `getOnboardingState()`, `signInWithEmail()`, `createHousehold(name)`, `joinHousehold(code)`, `saveMemberRestrictions(memberId, restrictions[])`, `savePreferences(memberId, prefs[])`; the fixture implementation keeps state in memory for the session (no persistence, no network). (e) **Contracts:** add `packages/contracts/src/household.ts` with `HouseholdDto`, `MemberDto`, `MajorAllergenCodeDto` (the nine codes, mirrored from `packages/domain/src/allergens/taxonomy.ts`), `RestrictionSeverityDto` (`standard` | `severe`), `MemberRestrictionDto` (`kind: MAJOR | USER_DEFINED`, `code?`, `label`, `severity`), `OnboardingStateDto`, plus a consistency test in `packages/adapters` proving the DTO code list equals the domain's `MAJOR_ALLERGEN_CODES` (the mobile app never imports domain).
- **Context:** prototype v4 `#scr-account`, `#scr-allergies` and the `startHousehold`, `toggleAllergen`, `renderSeverityList`, `addCustomAllergen`, `toggleNone`, `continueAllergies`, `skipPreferences` functions (the binding behaviour); [copy-deck.md](docs/design/copy-deck.md) §2 (voice, member attribution, allergen names), §10 (forbidden words); [design-principles.md](docs/design/design-principles.md) P5, P8, P9, P10, P11; [tokens.md](docs/design/tokens.md) §4 (danger on allergy-selection controls is a deliberate, PROPOSED call: selected allergen chips and the Severe button may use `danger`, nothing else may), §5 (44pt), §6 (motion); [ux-plan.md](docs/design/ux-plan.md) §2 rows S1 and S2, §3 flow 1; D-017 (P4: severe stays severe; the client never downgrades), D-022, D-023, OQ-D5; M3-T1 `tokens.ts`, `TabBar`, `ApiClient`.
- **Dependencies:** M3-T1 (done). Independent of M2-T1's endpoint (no household API exists yet; M2-T3 will replace the fixture methods behind the same port).
- **Invariants:** the explicit-none gate cannot be bypassed by any path (Continue, Skip preferences, back, deep link); "none" copy is exactly "No known allergies for {member}", never "no allergies", "safe", or a bare checkmark as clearance (copy-deck §10; the check icon on that option is a select affordance and must carry the text); no string in the app contains a forbidden word or an em dash (add a test that scans every string literal under `apps/mobile/src` and `apps/mobile/app` for copy-deck §10 terms and U+2014, with the caveat sentence as the one allowed `safe`); severity is never inferred, always chosen (default Standard shown as a choice, not hidden); colour is never the only signal (Severe reads "Severe" and carries an icon); every control is at least 44pt; the client never imports `@smart-kitchen/domain` (boundary lint); no new dependency without escalation (rule 11); no persistence layer yet (in-memory fixture only); no secrets; `apps/mobile` still exports green.
- **Acceptance criteria:** new-user flow S1 to S2 to Home works in Expo Go with the email button and either create or join; join with a wrong code shows the honest error and stays on S1; on S2 the Continue button is refused with the inline message until each of Dean and Maya has a restriction or the explicit none; selecting an allergen clears the none option and vice versa; every selected allergen shows a Standard / Severe choice; a custom allergen appears in the list at Standard; Skip preferences still enforces the gate; returning user lands on Home; all copy matches prototype v4 word for word except where copy-deck §2 requires sentence case; the forbidden-word and em-dash scan passes; contracts consistency test passes; root `pnpm lint typecheck test format:check` and `pnpm --filter mobile export` green from an empty build state (delete `dist` and `*.tsbuildinfo` first, run in CI order).
- **Tests required:** pure state-machine tests for the S2 gate (every member covered; none vs selections mutual exclusion; severity default and change; custom allergen; skip preferences still gated); fixture `ApiClient` tests for create/join/wrong code/state transitions; route-guard test (new vs returning user) as a pure function over `OnboardingStateDto`; contracts consistency test against the domain taxonomy (in `packages/adapters`); the copy scan test; no React Native testing library unless escalated (keep components thin, logic in pure modules).
- **File scope:** `apps/mobile/**` (new screens under `app/onboarding/*` and `src/onboarding/**`, `src/api/client.ts` extension, `src/screens/**`), `packages/contracts/src/household.ts` (new) and `packages/contracts/src/index.ts` (export), `packages/adapters/src/contracts-consistency/**` (new test only), `tests/fixtures/identity/README.md` (document the fixture join code), `docs/handoff/M3-T2.worker.md`.
- **Out of scope:** real authentication or any vendor; household or member API endpoints (M2-T3); persistence across app restarts; the Home dashboard states (held, OQ-D9) beyond reusing the M3-T1 placeholder with the first-run copy; S12 profile editing of allergies (M3-T6, which reuses S2's components); invites; dark mode; a UI library.
- **DoD:** rule 26; worker report lists any dependency added, states plainly whether Fraunces/Inter weights were checked on an Android device (M3-T1 follow-up) or not, and proposes any copy-deck additions (S1/S2 strings are not yet in the deck; propose a §11 for the architect to add at acceptance).

#### M3-T3 — Inventory list (S4) and item detail with ledger history (S5), HTTP read client against M2-T1, correction and removal behind the client port ✅ DONE 2026-09-22
*Sonnet worker (isolated worktree; stalled once mid-ticket and was resumed on its own branch) → independent Opus review (**PASS WITH FIXES**: the "float never enters" test did not discriminate; the §7 location-empty state was unreachable; a failed correction reported success and offered to undo an unrelated row; removal reasons rendered as "Discarded in Spoiled"; no cold-start failure path; plus twelve smaller) → fixes → re-review **PASS** → squash-merged `b41b809`. Ledger core mutation-verified: append-only, BigInt sums, clamp row system-authored and set only by the module, undo as a compensating row, chicken-breast fixture reproduces the prototype ledger. Architect rulings: rows keep the prototype's compact chips with the full label as accessibility label and the AI chip performs Confirm (deck §4 amended); the legend keeps the prototype sentence, added to §10's named negations; history list newest-first with a chronological "why" narrative; "Cooked in {recipe}" (deck §5); removals render an action label plus a `reason:` caption; the "not in mockup" toasts do not ship (search hidden until M4, tray Edit opens S5). Scope ratified: deleted adapters fixture module and test, `packages/contracts/src/inventory.test.ts`, lockfile. 1222 tests without a DB. Handoff: [worker](docs/handoff/M3-T3.worker.md) · [review](docs/handoff/M3-T3.review.md).*
- **Implementation model:** Sonnet. Two screens inside the established shell and tokens; the only logic is display formatting of exact quantities and a small append-only fixture ledger.
- **Review model:** **Opus-class**. Ledger semantics are on screen (rule 23: inventory arithmetic and reconciliation): the reviewer checks that no float touches a quantity, that corrections and removals are appended rows (never edits, rule 10), that undo is a compensating row, that the clamp row is visibly system-authored, and that provenance chips follow copy-deck §4.
- **Objective:** in `apps/mobile`, build S4 and S5 from prototype v4 (`#scr-inventory`, `#scr-item`, `#scr-legend`, and the functions `filterLoc`, `sortByExpiry`, `filterNeedsConfirm`, `stepItemQty`, `correctQty`, `pickReason`, `applyReason`, `prependHistoryRow`, `updateWhyPlain`). (a) **S4 Inventory list:** header with item count, a "what do these mean?" link to the legend; location segmented control (All, Fridge, Freezer, Pantry) with group headers; "Sort · expiry" and "Needs confirmation" chips; the "Needs your confirmation" tray for AI-tier rows (`Confirm` and `Edit`, copy-deck §6); rows with thumbnail placeholder, freshness ring (time-to-expiry ladder green, amber, rose; never danger), name, exact quantity, expiry urgency text (`use today`, `2 days`, `2 weeks`, `2 months`), provenance chip per §4; states per copy-deck §7 S4: empty per location, filtered-empty, and the stale-offline banner (rendered when the read client reports a cached result); first-run empty from the prototype ("Nothing here yet" / "Every item you add shows its provenance, so you always know what is a verified fact and what is a guess." / "Scan a barcode"). (b) **S5 Item detail:** large quantity with unit and location; the "Why {qty}?" plain-language line built deterministically from the last rows (copy-deck §5 template); "Correct this" card with a stepper and "Save correction" (prototype copy: "One tap if this number is wrong. A correction teaches the system. It is never an apology."); "Use or remove" chips (Consume, Discard, Mark expired, Donate) then a reason row (Spoiled, Wrong item, Used it all, Other); Lots with expiry and tier; History as ledger rows in sequence order, each with type label, actor initials chip, source, tier chip, time and signed delta; the clamp row rendered as a system row with copy-deck §5's clamp string, never attributed to a person; undo toast ("Corrected. Undo") where undo appends a compensating ADJUSTMENT. (c) **Legend screen:** the three §4 legend lines in tier order plus the closing line. (d) **Read client:** an `HttpApiClient` implementing the port's read methods against `GET /v1/inventory/items` with the fixture bearer token, base URL from `EXPO_PUBLIC_API_URL` read in one exempted `src/config/env.ts`; used when the variable is set, otherwise the fixture client; the client consumes M2-T1's `InventoryItemSummaryDto` and the deprecated M3-T1 placeholder types are removed from contracts and the app. (e) **Write methods, fixture only (M2-T2 supplies the endpoints):** `correctQuantity(itemId, newAmount)` appends an `ADJUSTMENT` with the signed delta; `removeQuantity(itemId, action, reason)` appends `CONSUME` / `DISCARD` / `EXPIRE` / `DONATE` per the copy-deck §5 mapping (a removal of the full on-hand amount); `undo(transactionId)` appends the compensating `ADJUSTMENT`; the fixture keeps an in-memory append-only history per item, derives the quantity by summing deltas in micros (`BigInt`, never `Number`), and reproduces the Chen household's prototype history for chicken breast including the `OVER_CONSUMPTION` clamp row; `confirmAiProposal(itemId)` flips the AI-tier row to confirmed (fixture only). (f) **Contracts:** `InventoryTransactionDto` (`transactionId`, `type` from the eight `TransactionType`s, `deltaMicros` as decimal text, `amount` text, `recordedAt`, `actor` as `{ kind: "user" | "system" | "ai-confirmed", displayInitials? }`, `provenance: FieldProvenanceDto`, `systemFlag?: "OVER_CONSUMPTION"`, `correlationLabel?` such as a recipe name) and `InventoryItemDetailDto` (summary plus `history`), with a consistency test in `packages/adapters` proving the DTO type list equals `TRANSACTION_TYPES`. (g) **Cleanup:** remove the unused `@smart-kitchen/adapters` dependency and tsconfig reference from `apps/mobile`; the screens tolerate a one-frame mount before the layout redirect (M3-T2 follow-up).
- **Context:** prototype v4 sections and functions named above; [copy-deck.md](docs/design/copy-deck.md) §4 (chips and legend), §5 (correction, reason mapping, clamp string, "why" template), §6 (tray heading), §7 S4 states, §8 (user-facing ledger errors incl. the generic fallback), §2, §10, and the precedence rule in its header; [design-principles.md](docs/design/design-principles.md) P2, P3, P6, P7, P8, P9, P11; [tokens.md](docs/design/tokens.md) §3 (terracotta scarcity), §4 (rose is urgency, never allergen; danger never on inventory), §5 (44pt: `.item`, chips, stepper, `.leglink`), §6 (drop transform transitions under reduced motion); [ux-plan.md](docs/design/ux-plan.md) §2 rows S4, S5; [ADR-008](docs/adr/ADR-008-inventory-ledger.md) and domain-model.md (ledger semantics, one unit per item, clamp policy); `packages/contracts/src/inventory.ts` (M2-T1 DTOs, exact micros as text); `apps/api/src/http/inventory-snapshot.ts` (what the endpoint returns); M3-T1 and M3-T2 code and reviews; D-021, D-023; OQ-1 (FEFO PROPOSED) for the lot ordering.
- **Dependencies:** M3-T1, M3-T2, M2-T1 (all done). The HTTP path is verified with a mocked `fetch` against contracts-shaped fixtures; a live run against a local API additionally needs seeded rows (see M2-T2 outline: dev seed command), so the ticket does not require it.
- **Invariants:** no quantity is ever a JavaScript `number` (micros are `BigInt`, display uses the `amount` text or exact decimal formatting of micros; a test proves a float never enters); corrections and removals append rows and never edit or delete one (rule 10); undo is a compensating row, never a deletion; the clamp row is system-authored (system icon plus "auto" text, never colour alone) and uses the §5 clamp string; every fallible value wears a tier chip and the AI chip is a `Confirm` action (§4); `danger` colour never appears on inventory surfaces, `rose` only for use-today urgency; no forbidden word or em dash (the M3-T2 copy scan covers the new files); every control 44pt; colour never the sole signal; the mobile app never imports `@smart-kitchen/domain`; no new dependency without escalation (rule 11); the `process` lint restriction stays, with `src/config/env.ts` as the single exempted file for `EXPO_PUBLIC_*` reads; no secrets.
- **Acceptance criteria:** S4 renders the fixture household grouped by location with counts, chips and tray as in prototype v4; the segmented control filters, "Sort · expiry" orders soonest first within groups without stranding headers, "Needs confirmation" shows only AI-tier rows; empty, filtered-empty and stale-offline states render the §7 copy; tapping a row opens S5 with the exact quantity, the "why" line, lots and history in sequence order; saving a correction appends an `ADJUSTMENT` row at the top of history, updates the quantity exactly, shows "Corrected. Undo", and undo appends the compensating row; a removal with a reason appends the mapped transaction type, zeroes the quantity, and returns to S4; the chicken-breast fixture reproduces the prototype ledger (+2.0, −2.25, clamp +0.25 system row, +1.25 = 1.25 lb); with `EXPO_PUBLIC_API_URL` set the list is read from the endpoint with the bearer token (mocked-fetch test) and a network failure shows the stale-offline banner with the last result; the legend renders §4 verbatim; root `pnpm lint typecheck test format:check` and `pnpm --filter mobile export` green from an empty build state in CI order.
- **Tests required:** pure formatting tests (micros text to display amount for the units in the fixture, including `3 × 16 oz` style count-of-packs and `8 of 12` where the DTO carries them; never via `Number`); fixture ledger tests (append-only, derivation by BigInt sum, clamp row present, undo compensates, removal mapping to `TransactionType`, AI confirm); HTTP client tests with a mocked `fetch` (bearer header, DTO parse, error to stale banner state; no real network); contracts consistency test for the eight transaction types; "why" line generator tests against the prototype example; copy scan extended to the new files; route/tolerance test that S4 renders safely when the layout redirect is pending.
- **File scope:** `apps/mobile/**` (new `app/inventory.tsx` content, `app/inventory/[itemId].tsx`, `app/legend.tsx`, `src/inventory/**`, `src/api/**`, `src/config/env.ts`, `package.json`, `tsconfig.json`), `packages/contracts/src/inventory.ts` and `index.ts` (new DTOs, remove deprecated placeholders), `packages/adapters/src/contracts-consistency/**` (test only), `eslint.config.js` (only the single-file exemption for `src/config/env.ts`), `apps/mobile/README.md` (how to point the app at a local API), `docs/handoff/M3-T3.worker.md`.
- **Out of scope:** write endpoints (M2-T2); the barcode and manual add flows (M3-T4); Home dashboard states; the Menu tab; persistence; search (the prototype's search icon toasts "not in this mockup"); expiry estimation logic (M8); photo thumbnails beyond the placeholder tiles (A11); a UI or icon library.
- **DoD:** rule 26; worker report lists dependencies (expect none), commands from an empty build state, deviations, escalations, and proposes copy-deck §7 S5 states and any new strings for the architect to add.

#### M2-T2 — outline (to be written after M3-T3): write endpoints over the ledger (`POST /v1/inventory/items/{id}/transactions` for ADJUSTMENT and the removal types, idempotency key from the client, retry helper, INV-TENANT-1 over writes), a dev seed command (`pnpm --filter api db:seed:fixture`: the fixture identities plus the Chen household's prototype inventory and chicken-breast ledger, refused in production like the identity adapter), and moving `readInventorySnapshot` into `db/inventory`. Opus-class implementation and review.

#### Pending D-024 (Dean's additions, 2026-09-22; see docs/po/dean-additions-2026-09-22.md): if accepted, M3-T3 gains a follow-up ticket (move item Fridge ⇄ Freezer as a recorded location event; "Remove from inventory" wording and hide rule; "Always keep N on hand" setting), M3-T5 adds allergen verdict chips on shopping rows and the origin display call, M3-T6 adds the unit-of-measure preference, M2-T2 adds the `MOVE` event and `minOnHand`, M7 adds executed-in-full with category default locations and the par-level gap rule, and the Menu tickets add favourites and the defrost card.

#### M3-T4 to M3-T6 — locked-screen build tickets (to be written one at a time as the predecessor is accepted)
M3-T4 add food S6 to S9 incl. camera permission states and the manual fallback (Sonnet / Sonnet; barcode library spike inside); M3-T5 shopping S11 with origin chips (menu gap, AI proposal, member) and offline queue (Sonnet / Sonnet); M3-T6 profile S12 (Sonnet / Sonnet). Each ticket's binding inputs: prototype v4, copy-deck.md, tokens.md tap-target and motion tables, and the M3-E0 follow-ups listed under "Accepted follow-ups". **Binding UI copy requirements for allergen surfaces:** the nine rules in [M1-T4's worker report §6](docs/handoff/M1-T4.worker.md) (never "safe"; the standing NO_SAFETY_GUARANTEE caveat always shown; unknowns never collapsed into allowed; critical warnings prominent; copy keyed off codes not messages; blocked evidence displayed; per-member attribution) **plus (M1-T6 review F7): warnings render regardless of verdict — an `ALLOWED` result can still carry `UNRECOGNIZED_ALLERGEN_DATA`.** *Exit:* MVP acceptance journey steps 1–3 pass on two devices.

### M4 — Barcode/product enrichment
Live lookup cascade per ADR-006 outcome + caching + our-catalog merge (normalization v1), manual completion UX for misses, barcode-resolution telemetry. *Exit:* measured resolution rate on real scans; catalog rows carry per-field provenance.

### M5 — Receipt ingestion + confirmation (fast-follow flagship)
Fixture corpus incl. §18B hard cases → pipeline stages behind ports → vendor bake-off (R-2, ADR-007 decision; cost approval gate) → upload (content-hash dedupe, INV-RCPT-1) → confirmation screen → PURCHASE transactions with correlation refs; object storage (ADR-009 decision); retention policy implemented (PO decision required first).

### M6 — Recipe generation + recommendations
`RecipeGeneratorPort` + first LLM vendor pick via eval suite (ADR-005 process; cost approval gate), schema-validated generation, deterministic ranking (§4 factors) + allergen screening integration via `partitionByVerdict` (M1-T4 — BLOCKED never displayed as a choice; a severe+unknown filtering policy decided here per D-017 P4), accept/reject telemetry, prompt-injection adversarial evals, **suspicious-character detector** (mixed-script/unexpected-codepoint ingredient text ⇒ warning + refuse to license absence — D-017 accepted-residual-risk mitigation), M1-T4's UI copy rules bind here too. *Exit:* INV-ALRG-1 E2E green; acceptance-rate metric live.

### M7 — Shopping-list closed loop
Gap computation (INV-SHOP-1) from accepted recipes + manual items, BUY/ALREADY HAVE, household sharing, offline cache + queued check-offs (ADR-010 scope), check-off → inventory prompt (close the loop). *Exit:* full MVP acceptance journey passes.

### M8 — Consumption + reconciliation
Meal logging → per-ingredient `USE_IN_MEAL` decrements (INV-MEAL-1), expiration estimates from shelf-life data + "use it soon" surfacing, reconciliation prompts ("still have this?"), calorie display (deterministic, INV-NUTR-1). *(Pulled from fast-follow list as evidence dictates.)*

### M9 — Hardening & observability completion
Alerting, cost caps enforcement, load/perf pass, security deep-set (isolation fuzzing, abuse limits), deletion/export flows, pre-launch legal gates (R-3/R-4/R-5), beta readiness review.

---

## Accepted follow-ups from completed tickets
- From M0-T1 (worker report, triaged at acceptance): Windows long-path CONTRIBUTING note → folded into M0-T2 (✅ done); `apps/api` dev/watch script (`tsx`-style) → add with first real API ticket (M2); OneDrive-sync concern → tracked in STATUS.md.
- From M1-T1 (worker §9 + review, triaged at acceptance 2026-09-08): **lot-selection policy (FEFO/FIFO) for consumption** — domain helper + UX input, needed before meal-log decrements (M2/M8, Opus); correction-rate telemetry query over `OVER_CONSUMPTION` clamps + user adjustments (after M1-T2); wiring units conversion into inventory/shopping services — incl. where `ConversionBridge` data (densities, per-item weights) lives and a possible `wasClamped` field on gap results — is **M2's job** (retargeted from an earlier note; M1-T3 deliberately left the ledger's pass-through `Unit` untouched); boundary schema validation belt-and-braces + who may claim a `system` actor + mustUse-style handling of discarded `AppendResult`s (M2); observation-boundary invariant test for unconfirmed AI-tier rows (M6); timestamp normalisation note added to ADR-010.
- From M1-T3 (review, triaged at acceptance 2026-09-09): tighten the domain boundary lint or its comment — `import-x/no-extraneous-dependencies` permits devDependencies by default, so a `fast-check` import into domain *production* source currently lints clean (small M0-maintenance ticket, fold into next config-touching work); `fl-oz` disambiguation guard if fluid ounces ever needed (never overload `oz`).
- From M1-T2 (worker §11 + both review rounds, triaged at acceptance 2026-09-10): **M2-blocking:** dedicated non-superuser owner role for SECURITY DEFINER functions (deployment-roles decision); retry helper must handle `23505` AND `40001`; managed-provider migration behavior (CREATE ROLE/definer ownership without superuser) verified against the chosen host. **Maintenance:** pin `postgres:17-alpine` by digest; lot-instant canonicalisation on write (+ property-generator instants); move pure helpers out from behind the DATABASE_URL gate; add a `next_sequence`-drift read-path test; reconciliation alerting job (INV-LEDGER-1 in prod = page, per ARCHITECTURE §8); UUIDv7 generation helper; CONTRIBUTING note on local DB options (native throwaway cluster pattern / Docker when installed). **Unblocked now:** correction-rate telemetry query (D-003 + schema both landed).
- From M1-T4 (worker §12 + both review rounds, triaged at acceptance 2026-09-10): **MANDATORY pre-M4 gates:** (a) malformed-`allergens`-container inputs must record as uninterpreted rather than silently dropping (blocks a declaration-assisted ALLOWED; ~3 lines + tests — must land before any adapter can mint an AllergenDeclaration), (b) decide who may mint a `KNOWN_FACT` AllergenDeclaration (D-017 open item). **M4:** non-FDA allergen codes (mustard/celery/lupin/sulphites/mollusc); coconut split out of tree_nut (pulled forward per review); adapter→domain completeness mapping (with the FDC-allergen-gap rule). **Maintenance:** corpus-mirror consistency check from packages/adapters; sesame product fixture; broader lookalike/negative-matching test corpus; extract shared deepFreeze when a third module needs it. **M9 gate:** clinical/domain-expert review of the curated allergen term lists before launch.
- From M1-T5 (worker §10 + review, triaged at acceptance 2026-09-09): **follow-up R-1 FDC measurement pass** (slower pacing or a free signup key — key acquisition needs PO approval per rule 17 — before ADR-006 finalizes FDC's slot); **M4: curated PLU → CanonicalIngredient table** (never the live cascade — settled sub-decision, D-010); **M4: FDC-allergen-gap rule** (FDC-only-resolved products render allergen status unknown + warning) — explicit implementation requirement; **M4: guard name-search against free-text collisions** (the "baby carrots → infant formula" class — category-scoped search); scripts/** eslint modernization (ecmaVersion/globals) maintenance ticket.
- From M3-E0-T3 (review opinions + worker gaps, triaged at acceptance 2026-09-16): **PO visual decision (with the Dean session or right after):** adopt or reject the seven PROPOSED token darkenings in tokens.md §2 (`--ink-3`, `--amber`, `--green`, `--rose`, `--danger`, brand-tint pairing, CTA text on `--brand-deep`) — every current FAIL is body/alert text on real surfaces incl. the Estimated badge, expiry text and the allergen bar; **M3 token-module ticket:** systematic pass over every `color: var(--X)` declaration lacking a co-located background (the review sampled the highest-traffic ones); apply the 44px remediation table and the reduced-motion table; `.amt.pos` green on sand (4.04) if that combination ships; photo-luminance assumption for scrims to be re-checked on real imagery.
- From M3-E0-T1 (review F10–F17 + opinions, triaged at acceptance 2026-09-16): **design/PO decision (M6):** whether unconfirmed AI-tier inventory rows count toward recipe coverage scores and FEFO cook plans — the prototype currently counts them (relates to the M1-T1 observation-boundary follow-up). **M3-E0-T3 inputs:** measured tap-target shortfalls (`.leglink` ~15px, `.toastUndo` ~24px, `.chk` 26px, `.okbtn` 32px, chips 34–37px, `.seg` 36px) → 44px remediation table; `prefers-reduced-motion` guard for the laser sweep and screen-in animation; `--danger` on allergy-selection controls and the green-check "none" option → deliberate calls in the semantic-colour table; `--ink-3` dot on the espresso hero recorded as decoration; viewfinder brackets + laser recorded as terracotta exceptions. **M3 build tickets:** `aria-label` coverage (only two today); sort/filter leaving group headers stranded; a recipe → shopping-list gesture (flow 3's missing hop); "No match" must not reuse the Estimated tier chip; FEFO shown as a multi-lot split somewhere; expandable "N members screened" list on ALLOWED; consistent prepend/append ordering in S5; dead CSS (`.sevrow`); S4 stale-offline state (still undesigned).
- From M3-T3 (review, re-review, worker deviations; triaged at acceptance 2026-09-22): **M2-T2:** a dedicated `reason` field on `InventoryTransactionDto` (removal reasons currently ride in `correlationLabel`); an idempotency key on undo and every write (the fixture's `undo` is not idempotent); the item-detail read endpoint (`HttpApiClient` serves history only from the fixture until then); lots derived from the real ledger on every read (the fixture's lots are a static snapshot, OQ-1); a pack-size field on the wire (the client infers "3 × 16 oz" from equal lots and "8 of 12" from a lot label). **M3-T4:** architect approves adopting `@testing-library/react-native` as a devDependency (rule 11 escalation recorded here; the M3 screen set otherwise ships with no component-level tests) and the first component tests cover the pre-redirect frame on S4 and S5; a global toast host in the layout so a removal can offer Undo across the S5 to S4 navigation; Confirm reachable from S5 for an AI-tier item opened by deep link; the `metro.config.js` comment that workspace packages resolve through `dist` (retargeted from M2-T1 acceptance). **S5 design pass (M8 or earlier):** an expired state ("use today" currently covers negatives), documented in deck §7 S5 as owed. **PO token call (tokens.md §4, with the none-option colour):** green amounts on sand (`amtPositive`, 4.04:1) are now on a real screen. **Accepted:** 7-day amber to green ring boundary as an ENGINEERING INFERENCE; static lots; no removal undo in this ticket.
- From M3-T2 (review, two re-reviews, worker judgment calls; triaged at acceptance 2026-09-22): **M3-T3:** the inventory screen must not assume the gate is closed at mount (a warm deep link paints the destination for one frame before the layout redirect); remove the now-unused `@smart-kitchen/adapters` dependency and tsconfig reference from `apps/mobile` (the client no longer imports it; the M2-T1 bundling blocker is discharged); a copy-deck §7 pass against prototype v4 for S4/S7/S10/S11 before build, applying the precedence rule; replace the fixture inventory literals with contracts-shaped JSON captured from `GET /v1/inventory/items`. **M3-T6 (S12):** per-member preferences (M3-T2 saves one shared set to every member as an MVP simplification); `onSubmitEditing` on the custom allergen input; `firstName()` for single-token or multi-part given names; reuse of the S2 components. **M2-T3:** the household/member endpoints must carry `noneConfirmed` explicitly (OQ-E1 rule applies). **PO calls (tokens.md §4):** the green tint on the confirmed "No known allergies" option; chip order (follows `MAJOR_ALLERGEN_CODES`, not the prototype). **Device checks owed:** hardware back and edge swipe on S2, Fraunces/Inter variable weights on Android, the nav bottom offset. **Accepted:** `NodeJS.*` types stay visible in app code (runtime globals are lint-restricted); lockfile `@types/react` dedupe.
- From M2-T1 (review + re-review notes + worker §11, triaged at acceptance 2026-09-22): **M3-T3 blocker (latent, not a regression):** importing `FixtureApiClient` from any screen fails `expo export` because the `@smart-kitchen/adapters` barrel drags `node:fs`/`node:url` (product-lookup fixture paths) into the bundle; fix by dropping the adapters dependency from `apps/mobile` in favour of contracts-shaped JSON fixtures (already planned) or, at minimum, an `./inventory` subpath export; and make the CI export smoke check meaningful (a screen must import the client). **M3-T3 / apps/mobile:** one comment in `metro.config.js` that workspace packages resolve through `default` (`dist`) and need a build first; re-check Metro's condition set when the bundle first includes them; the fixture household ids diverge (`sessions.json` UUIDs vs M3-T1's `hh-fixture-chen`), converging when the client reads the endpoint. **M2-T2:** move `readInventorySnapshot` SQL from `apps/api/src/http/` to `db/inventory/`; consolidate `SK_APP_ROLE` into one shared constant (production must not import test support); the app role keeps INSERT and column-scoped UPDATE the read does not need (revisit when writes land); route-registration throws inside `register` surface via avvio as an uncaught exception (consider wrapping). **Maintenance:** COMMIT-tag check and guarded ROLLBACK in `seed-fixture-identities.ts`; note in the redaction denylist that `displayName` also matches the item DTO field; note in `tenant-session.ts` that the role pin is containment, not a sandbox (`RESET ROLE` is possible from inside). **Recorded in DECISIONS:** OQ-E1 multi-household sessions (M2-T3 rule), OQ-E2 lot expiry provenance (with OQ-D7); D-022 wording corrected.
- From M3-T1 (review F2 to F4 + opinions, triaged at acceptance 2026-09-22): **M3-T3:** switch the mobile fixture `ApiClient` from `packages/adapters` ledger-built fixtures to contracts-shaped JSON captured from the M2-T1 endpoint, and drop `@smart-kitchen/adapters` from `apps/mobile` so no domain code ships in the client bundle (the adapters package now depends on domain at runtime); decide whether the ledger-built fixtures become the real `tests/fixtures/inventories/` corpus. **M3-T2:** verify Fraunces and Inter variable-font weights render on Android (unverified on device); visual check of the nav bottom offset (`18 + insets.bottom`) on a real phone; promote the type-scale and spacing ENGINEERING INFERENCE values in `tokens.ts` to DOCUMENTED as screens consume them. **Scope ratified:** `packages/adapters/{package.json,tsconfig.json}` edits (domain to dependencies, contracts added, project reference).
- From M3-E0-T2 (review notes + worker items, triaged at acceptance 2026-09-16): **for the M3 string-table ticket:** `StorageLocation` display strings (FRIDGE/FREEZER/PANTRY/OTHER — decide "Other") are user-visible and owned by no document; accessibility-label strings for every verdict and chip per the deck's new P9 rule; allergen-name pluralisation/grammar pass ("contains peanut" vs "peanuts"); undo-window duration as a product parameter. **PO/PRD:** ratify the reason-chip → `TransactionType` mapping ("Cooked" = `USE_IN_MEAL`, "Used" = `CONSUME`) when the consumption flow is designed; OQ-1/A9 shortfall wording before M8 (already in the decision brief).
- From M1-T11 (review F2/F4/F5 + worker §6, triaged at acceptance 2026-09-16): **M2-blocking companion (small, Opus review):** make the COMMIT tag check fail-closed (`command !== "COMMIT"` ⇒ throw) and update `retry.test.ts`'s fake-pool stubs to return a `command` (review F2); add a lint rule or review-checklist line forbidding catch-then-return-success after a database error (worker item 2); a structural "requires a transaction" guard on `appendTransactionToDb` (worker item 1) in M2's first write-handler ticket. **Maintenance:** savepoint-hygiene oracle should assert "no savepoint at all" rather than the literal name (F5); note the retry helper's reach wherever it is documented — `TransactionAbortedAtCommitError` is non-retryable by design (F4); test-suite database churn (44 files × create/migrate/drop) — harness/vitest tuning, low urgency (did not reproduce for the reviewer).
- From M1-T10 (review F1 + judgment call (i), triaged at acceptance 2026-09-15): **small, Sonnet, before `packages/domain` gains another consumer:** move the recommendations-corpus re-export from domain's main barrel (`src/index.ts`) to a subpath export (`"./allergens/corpus"` in domain's `package.json` exports map) so the production API surface is exactly the engine; update the adapters consistency test import. **Note (no ticket):** the lot-instant canonicalisation in `insertInventoryLot` is defence-in-depth with no observable effect today (Postgres normalises `timestamptz` on read; lots have no in-memory replay comparison) — its test cannot discriminate an accidental revert; revisit only if a string-compared lot path appears.
- From M1-T8 (worker §8 + review F2/F5 + opinions, triaged at acceptance 2026-09-15): **ledger hardening (small, Opus review, before M2 read model):** teach `reconcile()` the duplicate-`lotId` check and shape-guard its inputs so `planLotConsumption`/`reconcile` are total on malformed aggregates (review F2 — five `TypeError` shapes; root cause in `derive.ts`); make the planner's final tie-break intrinsic (`lotId`) rather than positional so plans are a pure function of lot data before any second read path exists (F5); extract the duplicated ISO-instant parser and `copyActor`/`copyProvenance`/`copyCorrelationRef` trio from `ledger.ts`/`lot-selection.ts` into shared internals (worker item 1); shared adversarial-getter probe helper in domain test support. **PO/PRD before M8:** shortfall policy for meal-log decrements — record the full statement and accept the clamp vs refuse and prompt (worker item 2; also OQ-1). **M2:** surface `skippedLots` and per-allocation lot metadata in the read model (item 3). **Only if the product asks:** expired-stock exclusion needs a caller-supplied `asOf` + PO decision (item 4). Performance note: planning is O(lots × rows) per item — fine at household scale; derive all lot balances in one pass if a bulk path appears (item 5).
- From M1-T7 (review F1–F5, B1–B4, triaged at acceptance 2026-09-14): test hardening B1/B2 → **M1-T10 item (l)**; **M2 deployment-roles note (B3):** `security_invoker` means a read-only dashboard/telemetry role needs SELECT on `inventory_transactions` too — granting the view alone is not enough; decide alongside the `SECURITY DEFINER` owner role; **whenever a second `SystemFlag` variant is proposed (B4/F2):** revisit `statement_count`'s `system_flag_kind IS NULL` predicate or the KPI denominator silently shrinks.
- From M1-T6 (worker §9 + review F1–F7, triaged at acceptance 2026-09-14): **pre-M4 allergen hardening (small, Opus review):** single-read `typeof` guards in `parseAssertions` (review F1 — a double-read getter can make `screenSubject` throw; fails loud, not open) + pin `detail` strings for `[undefined]`/sparse holes (F6) + report **all** uninterpreted entries per locus instead of the first (worker item 2; changes `unknowns` cardinality — needed before an M3 "what is unknown" surface). **M4 (mandatory, alongside gate b):** reject malformed or *misplaced* `allergens` payloads at the product-lookup port boundary (worker item 4 + review F4 — a recipe-level `allergens` field is invisible to the engine). **M4 (optional):** warn-only term scan of malformed entries' raw text — never a `MATCH`, never `ALLOWED` (worker item 1). **Copy rule addition (M3/M6):** `ALLOWED` can co-occur with `UNRECOGNIZED_ALLERGEN_DATA` when no member holds a restriction — render the warning regardless of verdict (review F7). **M1-T10 (i):** fix the recommendations README sentence citing a non-existent `fixtures.test.ts` (worker item 3).
- From M0-T2 (worker report + review, triaged at acceptance 2026-09-08): bump `packageManager` pnpm 12.3.1 → 12.3.4 (one-line maintenance, next ticket that touches package.json); add `workflow_dispatch:` trigger to ci.yml (optional convenience, with that same change); SHA-pin the remaining actions (checkout/setup-node/pnpm-setup — hardening, fold into M9); after the owner applies branch protection, verify a real PR shows both required checks (architect acceptance step, next PR).

## Non-milestone track (continuous)
- Fixture corpus growth (every prod bug → fixture first).
- Docs hygiene: DECISIONS/STATUS updated with every merged decision-affecting PR.
- Cost telemetry review at each milestone exit.
