# Names only — no values, no secrets. See CLAUDE.md rule 12.
# Populated as each ticket introduces the config it needs; empty at M0-T1
# because no adapters, database, or auth integration exist yet.

# --- apps/api ---
PORT=
NODE_ENV=
